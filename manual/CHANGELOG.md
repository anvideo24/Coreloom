# 매뉴얼 변경 기록

- 2026-09-05: F07 자동 검사 결과 5건을 결과 파일에 덧붙였습니다(검증 대상 코드 커밋 a85c38a). F07-02·F07-04는 통과, F07-03·05·06은 자동으로 잰 부분만 적은 미검증입니다. F07-01은 대표가 PC·휴대폰에서 직접 세야 하므로 결과 없음으로 남깁니다.

- 2026-09-05: F07 개선 목표·검증 현황 조회 화면(`/admin/manual/progress` 상단 구역, 상세 `/admin/manual/progress/[기능ID]`)을 가지 `claude/f07-verification-view`에서 구현 중입니다. 결과 파일 `docs/quality/verification-results.json`을 새로 두어 검사 결과를 덧붙이는 자리로 삼았고, 이 파일과 계획서를 문서로 가리키는 규칙을 `RULES.md`·이 매뉴얼·`system-progress.md`에 추가했습니다. 첫 결과 4건(F02-01, F05-01, F05-02, F05-03)은 기준 측정에서 재현된 실패로 기록했습니다. **main 병합·배포, F07-01·05·06(대표가 직접 재는 항목), 다른 기능의 코드 변경은 이번 변경에 포함하지 않습니다.**
  - 자동 검사: 타입 검사 0건, 시험 283건 통과(검증 도메인 18건·읽기 계층 9건·실제 파일 통합 10건 포함), 운영 빌드 성공. 실제 계획서·결과 파일로 화면 부품을 정적으로 그려 360·840·1280px에서 가로 넘침 0, 링크 높이 휴대폰 48px·PC 24px 이상을 쟀습니다. 로그인한 진짜 화면은 아니므로 대표 확인이 남습니다.
  - 그 실측에서 결함 셋을 고쳤습니다. PC 표에서 「미착수」 같은 짧은 낱말이 세로로 쪼개지던 것, 상세 화면이 계획서의 검사 표를 아래 검사 표와 두 번 보여주던 것, 검사 표가 9열이라 칸이 글자 폭까지 쪼그라들던 것(6열로 줄이고 896px 아래는 카드).
  - `next build` 뒤 `npm test`가 빌드 산출물 안의 시험 사본까지 돌려 빨갛게 되던 것을 vitest 설정에서 제외했습니다. CI는 시험이 빌드보다 먼저라 드러나지 않던 자리입니다.

- 2026-09-05: 기능별 개선 계획·기준 측정 문서를 운영 매뉴얼과 진행 현황에 연결하고 Claude 인계 안내를 추가했습니다. 읽을 순서, 로컬 검토 브랜치, 미측정 항목, 다음 F07 구현 제안의 승인 경계를 기록했습니다. 제품 코드·DB·설정 변경 및 main·배포 반영은 포함하지 않습니다.

- 2026-09-05: 전송 직후 입력창을 비우고 실패 시에만 복원하도록 수정했습니다. 모든 메시지에 복사를 제공하며, 연결 끊김을 사용자 중지와 분리했습니다. GPT Terra 실사용에서 생성 중 새로고침 후 답변 복구, 양쪽 메시지의 실제 클립보드 복사, 명시적 중지 후 입력 복원을 확인했습니다. PC 프로세스 종료 복구는 범위 밖입니다.

- 2026-09-05: 대화·모델·작성 중 입력 복원, 여러 이미지 선택·붙여넣기·드롭·개별 제거, 실패 입력 보존과 요청 중복 방지를 추가했습니다. GPT Mini·Claude Sonnet 실행기에 합성 이미지 두 장을 전달해 색상 인식을 확인했습니다. 모바일 390px 폭에서 가로 넘침 없음과 주요 버튼 48px 이상을 확인했습니다. 이미지 보관은 서버 PC 전용이며 문서함 원본 자동 해석은 포함하지 않습니다.

- 2026-09-05: 에이전트별 ERP 읽기 권한과 PC 허용 폴더 옵션을 연결했습니다. 서버가 워크스페이스·프로젝트·경로를 검사하고, 읽은 자료의 출처 및 조회 이력을 남깁니다. 실제 개발 DB 견적 목록(0건) 조회와 합성 파일 검색·읽기를 구독 대화에서 확인했습니다. 테스트 PC 폴더 권한은 회수했습니다. PDF·이미지 해석과 변경·삭제·외부 발송은 범위 밖입니다.

- 매출 원장·분할 청구 헤더에 `+`가 둘 있던 것을 하나로 줄였습니다. `+`를 누르면 메뉴에서 매출/사업(또는 단건/반복)을 문구로 고릅니다.
- 2026-09-05: 대표가 PC 로컬 주소에서 로그인한 뒤 대시보드와 에이전트 페이지의 인증된 접근 및 런타임 오류 없음을 확인했습니다. 로컬 실제 계정 로그인 확인 대기를 해소했습니다.

- 2026-09-05: 대표 승인 후 데이터 DB를 인증과 같은 개발 브랜치로 전환하고 0029까지 적용했습니다. 이전 DB 자료는 변경하지 않았습니다. HTTPS 화면에서 테스트 에이전트의 GPT 답변·저장·새로고침 후 복원을 확인했습니다. 서버 재시작 후 두 로컬 주소 모두 Origin 차단 없이 인증 검증에 도달했으며 실제 계정 로그인은 사용자 재확인이 남았습니다.

- 🔴 **저장소를 공개로 바꾸고, `main` 병합에 검사 통과를 조건으로 걸었습니다.** 이제 CI가 빨간 PR은 합칠 수 없습니다. 비공개 저장소에서는 무료 플랜으로 이 기능을 못 써서 공개를 택했습니다. 바꾸기 전에 이력 전체를 훑어 비밀·실데이터·고객사명·실주소가 없음을 확인했고, 공개 전 커밋 26개에 개인 이메일이 작성자 정보로 남는 것은 대표가 알고 감수했습니다. **앞으로 이 저장소에 넣는 것은 전부 공개된다는 전제로 씁니다.**
- PR과 `main` 커밋마다 타입 검사·시험·빌드가 자동으로 돕니다. 켜자마자 셋이 드러났습니다. 타입 검사가 빈 기계에서만 깨지던 것(라우트 타입 미생성), 견적 PDF 시험이 15초를 넘기던 것(한국어 글꼴 심기가 원래 느림), `main` 커밋이 취소로 검사 없이 지나가던 것.

- 2026-09-05: AI 패널과 에이전트 상세에 공통 대화 화면을 연결했습니다. Codex·Claude 구독 CLI 직접 응답, 모델 선택, 대화 저장(0029), 새 대화·이력·중지·복사와 에이전트 지침 편집을 추가했습니다. GPT·Claude 실제 응답과 인증 차단을 검증했으며 DB 적용·로그인 후 화면 확인은 대기 중입니다. API 키로 전환하거나 회사 기록을 자동 실행하지 않습니다.

- 모바일에서 에이전트 패널 입력 키보드가 올라와도 헤더(닫기)가 가려지지 않게 했습니다. `visualViewport`에 맞춰 시트 높이를 줄이고, 헤더는 패널 상단에 고정합니다.
- 휴대폰 Funnel(HTTPS)에서 **웹앱으로 설치** 안내를 다시 보이게 했습니다. 서비스 워커를 보안 컨텍스트에서 등록하고, 좁은 화면에 설치 칩을 둡니다. Android·Chrome은 설치 프롬프트, iOS Safari는 홈 화면 추가 안내입니다. 이미 단독 앱으로 열린 때는 숨깁니다.
- **마이그레이션 안전장치가 실제 접속 대상을 보게 고쳤습니다.** 예전에는 「개발 DB를 향한다」는 표시만 확인했는데, 그 표시를 쓰는 두 곳이 **어떤 연결 문자열을 받든 늘 그렇게 적어서** 검사가 항상 통과했습니다. 운영 DB를 향해도 막히지 않는 상태였습니다. 이제 `CORELOOM_DATABASE_HOST`에 사람이 한 번 확인해 적은 접속 대상과 실제 접속처가 같을 때만 돕니다. 값이 없으면 거부하고, 지금 어디를 향하는지 보여 주며 Neon Console에서 확인한 뒤 적으라고 안내합니다. 돌기 전에 어디에 쓰는지도 화면에 남깁니다. **다만 처음부터 운영 주소를 넣고 그대로 확인해 버리는 것까지는 막지 못합니다** — 그건 사람이 Console을 봐야 합니다. 클라우드 에이전트를 쓴다면 이 값도 Secrets에 넣어야 서버가 뜹니다.
- 개발 DB 스키마 줄을 또 사실에 맞게 고쳤습니다. 0025–0028이 「적용 필요」로 적혀 있었지만 스키마를 조회해 보니 0000–0028이 전부 들어가 있었습니다. **오늘만 두 번째**라, 그 줄에 「돌리기 전에 스키마를 먼저 조회하라」는 경고를 붙였습니다.
- 매뉴얼 화면 셋(카테고리 홈·공용 정본·이 제품 규칙)을 대표가 로그인해 직접 확인했습니다. 진행 현황에서 「PC 확인 필요」를 「완료」로 닫았습니다.
- 전역 AI 패널을 Notion AI 채팅처럼 세로 공간을 넓히고, 하단 안내 문구를 없앴습니다. 답변은 짧은 안내와 패키지 복사·접힌 미리보기로 바꿨습니다.
- 전역 AI 패널을 갤럭시 폴드 커버처럼 짧은 화면에서도 닫을 수 있게 고쳤습니다. 시트를 상단 세이프에어리어와 하단 탭바 사이에 고정하고, 배경 탭·헤더 `×`로 닫히게 했습니다.
- 전역 AI 에이전트 패널 보내기 칸에 구독 모델 선택을 두었습니다. Claude/GPT·Codex/Cursor를 고르면 그 자리용 패키지가 만들어지며, 에이전트를 바꾸면 기본 모델로 맞춥니다. API 키 호출은 하지 않습니다.
- 배포본 포장을 두었습니다. Next `output: "standalone"`과 저장소 뿌리 Dockerfile로 이미지를 싸며, 진입점은 `apps/web/server.js`입니다. 절차 정본은 `docs/operations/deploy-packaging.md`입니다. `.env*`는 이미지에 넣지 않습니다.
- 전역 AI 에이전트 패널에서 구독 호출을 이었습니다. 요청을 보내면 구독 자리(Claude/GPT·Codex/Cursor)에 붙일 패키지를 만들고 작업 이력을 남깁니다. API 키 호출과 자동 실행은 하지 않습니다.
- 대표 승인함(`/approvals`)을 두었습니다. 비용·매출·입금 확정, 계약 체결, AI 제안, 에이전트 요청 대기를 한 목록에서 보고 상세로 이동합니다. 대시보드 승인 대기 숫자와 맞춰 두었습니다.
- 계정과목 마스터(`/accounts`, 0028)를 두었습니다. 자산·부채·자본·수익·비용 기본 과목을 시드하고, 매출·비용 원장에서 수익·비용 과목을 고릅니다. 복식 분개·전표는 포함하지 않습니다. 개발 PC에서 `npm run db:migrate`가 필요합니다.
- 고객사 거래 유형(매출/매입/동시, 0027)과 비용 매입처 고객사 연결을 넣었습니다. 기존 고객사는 매출로 두며, 매입처로 쓰려면 매입 또는 매출·매입으로 바꿉니다. 개발 PC에서 `npm run db:migrate`가 필요합니다.
- 필드 패스2(0026): 계약번호, 매출·비용 계정과목, 비용 매입처, 문서함 고객사 직접 연결을 넣었습니다. 개발 PC에서 `npm run db:migrate`가 필요합니다.
- 필드 패스(0025): 고객사 과세유형·계좌·통장사본, 계약 효력 기간·자동갱신, 청구번호·PO를 넣었습니다. 개발 PC에서 `npm run db:migrate`가 필요합니다.
- 개발 DB 스키마 상태 기록을 사실에 맞게 고쳤습니다. 「0019–0024는 아직 적용이 필요하다」고 적혀 있었지만, 스키마를 직접 조회해 보니 0000–0024가 이미 전부 `ai-development`에 들어가 있었습니다. 낡은 기록을 보고 마이그레이션을 다시 돌리려던 참에 드러났습니다.
- 운영 화면 리스트·패널 롤아웃을 계약·청구·매출·비용·설립 준비·에이전트까지 이어 맞췄습니다. 청구·매출은 생성 종류가 둘이면 `+`를 둘 둡니다. 화면별 외부 사례·남은 필드(과세유형·계약기간·PO·계정과목 등)는 `docs/operations/ops-screen-list-panel-rollout.md`가 정본입니다. 프로젝트는 요약·시작/목표일(0024)을 둡니다.
- 운영 화면 리스트·패널 롤아웃과 화면별 필드 격차를 `docs/operations/ops-screen-list-panel-rollout.md`에 정리했습니다. 프로젝트(`/clients-projects`)는 리스트+`+` 패널과 요약·시작/목표일(0024)을 둡니다. 업무·근거·AI 제안·문서함도 같은 목록+`+` 패널 UX로 맞췄습니다(필드는 유지).
- 고객사에 세금계산서·계약용 회사 정보(사업자등록번호·대표자·주소·업태·종목·홈페이지·대표 연락·사업자등록증 위치)를 두었습니다. 등록·수정은 구역을 나눈 2열 패널이고, 목록에는 사업자번호·대표자가 보입니다. 담당자에는 계산서 수신 여부를 두며 이메일이 필요합니다. 개발 DB에는 0023 마이그레이션이 필요합니다.
- 고객사를 `/clients`로 분리했습니다. 목록을 먼저 보여 주고 헤더 `+`로 작성 패널을 엽니다. 담당자는 고객사 상세(`/clients/[id]`)에서 추가합니다. `/clients-projects`는 프로젝트 목록·등록만 남겼고, 내비는 수주 아래「고객사」「프로젝트」로 나뉩니다.
- 진행 현황에 **고객용 견적서 포맷 수정**을 후속(보류)으로만 적어 두었습니다.
- Cursor Cloud Agent가 `/sign-in`까지 열어 화면을 눈으로 확인하는 절차를 정본에 넣었습니다. `apps/web`의 `npm run cloud:bootstrap`·`npm run cloud:dev`와 환경 Secrets 이름만 적고, 비밀 값·실이메일은 남기지 않습니다. PC의 `npm run up`은 그대로입니다.
- 회사 프로필 테이블(0022)이 아직 없는 DB에서도 `/quotes`와 `/company-setup`이 깨지지 않게 했습니다. 공급자·입금은 브랜드 기본과 빈 칸으로 보이고, 저장은 막으며 개발 PC에서 `npm run db:migrate`로 0022를 적용하라고 안내합니다. 적용 여부는 PC에서 확인합니다.
- **일을 시작하고 끝내는 법을 공용 저장소로 옮겼습니다.** `working-method`에 `AGENTS.md`를 새로 두고, 읽는 순서·되돌려 적는 자리·부트 문구를 그 한 곳에 모았습니다. Coreloom의 `AGENTS.md`에는 이 제품만의 기동(경로·`npm run up`·포트·마이그레이션)만 남겼습니다. 공용 `HOW.md`에 이미 있던 가지 규칙·범위 규칙·완료 판정이 Coreloom에도 또 적혀 있던 것을 걷어냈습니다. 매뉴얼 갱신 의무는 `RULES.md` 「문서와 관리자 화면」 절로 모았습니다.
- **운영 빌드(`next build`)가 되게 고쳤습니다.** 두 가지가 겹쳐 막고 있었습니다. 하나는 매뉴얼 원본이 앱 폴더 밖에 있는데 추적 기준을 올려 두지 않아 Turbopack이 설정을 거부한 것이고, 다른 하나는 시험 파일의 타입 오류였습니다. 앞의 것이 먼저 터져 뒤의 것은 보이지도 않았습니다. 이제 빌드가 통과하고, `manual/` 원본과 뿌리 `RULES.md`가 **배포 목록에 오르는 것**까지 확인했습니다. 다만 배포본을 실제로 싸는 설정(`output: "standalone"`·Dockerfile·Vercel)은 아직 없습니다. 목록이 만들어졌을 뿐 포장은 다음입니다.
- 배포 목록에서 `.env*`를 빼도록 막아 뒀습니다. 설정 파일을 읽는 라우트 때문에 `.env.local`이 목록에 들어가 있었고, 그대로 포장하면 연결 문자열과 API 키가 같이 실립니다.
- 견적 INVOICE에서 항목 6칸을 미리 확보하고, 부가세 안내·합계·입금/공급자 블록을 페이지 하단에 고정했습니다. 공급자·입금 정보는 `/company-setup`의 회사 프로필에서 읽어 오며 개발 DB에는 0022 마이그레이션이 필요합니다.
- `/admin/manual`을 긴 개요 문서 대신 **카테고리 카드 입구**로 바꿨습니다. 규칙·일하는 방식·운영 세 묶음으로 나뉘고, 긴 원본은 칸을 고른 뒤에 열립니다. 공용 규칙·일하는 방식·배운 것은 `working-method` 저장소 원본을, 이 제품 규칙은 저장소 뿌리 `RULES.md`를 읽습니다. 기존 개요는 `/admin/manual/overview`로 옮겼습니다. 탭 줄은 「매뉴얼 홈」으로 돌아가는 길로 바꿨습니다. 공용 저장소를 찾지 못하면 그 칸은 「저장소가 아직 없다」고 밝힙니다. 화면용 사본은 만들지 않았고 편집기는 아직 없습니다.
- 견적 INVOICE 글자·세로 간격을 줄이고, 설명 열을 넓히며 수량·단가 열을 좁혀 항목 6개까지 한 장에 들어가게 맞췄습니다.
- 견적 고객용 탭은 입력 없이 발송 문서 미리보기만 보여 주고, 가로는 A4 비율로 한눈에 들어오게 좁혔습니다. 사인은 배경 없는 PNG로 교체했습니다.
- 견적 작성·인쇄 화면을 PDF와 같은 A4 스위스 미니멀 `INVOICE`로 맞췄습니다. 고객용 탭은 항목·설명·수량·단가·공급가액 표와 발행일·유효기간·담당자를 편집하고, 오른쪽에는 실시간 합계와 버전 이력을 둡니다. 개발 DB에는 0021 마이그레이션이 필요합니다.
- 견적 PDF를 A4 스위스 미니멀 INVOICE 문서로 바꿨습니다. 제목만 영어 `INVOICE`, 본문 라벨은 한국어이며, 견적번호·발행일·유효기간·수량·단가·공급가액·입금 안내·공급자(사업자등록번호는 하단 한 곳)를 포함합니다. 개발 DB에는 0021 마이그레이션이 필요합니다.
- 공용 제품 규칙과 일하는 방식은 GitHub `anvideo24/working-method`로 두고, Coreloom `RULES.md`는 이 제품 오버레이만 남겼습니다. 공용 원문을 프로젝트에 붙여넣고 고치지 않습니다.
- 넓은 화면에서 호버로 들어오는 임시 메뉴는 작성 패널·에이전트 패널보다 위, 최상단 레이어로 고정됩니다. 클릭으로 연 사이드바는 이전처럼 본문을 옆으로 밉니다. 이 규칙은 `RULES.md` 8절이 정본입니다.
- 넓은 화면 접기/펼치기 버튼을 두 자리로 나눴습니다. 접혔을 때 펼치기 버튼은 왼쪽 위에 있고, 열린 드로어 안의 접기 버튼은 드로어와 같이 슬라이드합니다. 이 규칙은 `RULES.md` 8절이 정본입니다.
- 작성 패널 상단 「제목·닫기」 헤더를 없애고, 바깥(배경) 클릭이나 Esc로 닫히게 두어 본문 공간을 넓혔습니다.
- 견적 작성 패널을 문서 최상위로 띄워 메뉴 햄버거보다 위에 두었습니다. 상단은 견적명(왼쪽)·고객사·프로젝트를 한 줄로 두고, 역할·등급 선택 시 월 단가가 자동 입력되며, 금액 콤마·슬라이더 100% 표시도 고쳤습니다.
- 견적 작성기를 작업 패키지·내부 원가 중심으로 바꿨습니다. 넓은 패널에서 고객용/내부 원가 탭, 마진·운영비 슬라이더, 접힌 행에서도 수치 편집이 됩니다. 고객 PDF에는 작업명·설명·금액만 나갑니다. 개발 DB에는 0020 마이그레이션이 필요합니다.
- 전역 AI 에이전트 패널이 본문 폭을 줄이지 않도록, 우측 그리드 칸이 아니라 본문 위 플로팅으로 뜨게 바꿨습니다. 이 규칙은 `RULES.md` 8절이 정본입니다.
- 견적서·에이전트 목록을 리스트 중심으로 바꿨습니다. 생성은 헤더 `+`와 작성 패널로 엽니다. 견적 버전마다 부가세 포함/미포함을 고를 수 있습니다. 운영 화면 어디에나 우측(모바일은 하단) 전역 AI 에이전트 패널을 두었고, 등록된 활성 에이전트와 구독 모델·현재 화면 컨텍스트를 고릅니다. 에이전트에는 지침·절차·능력(기본 꺼짐)을 둡니다. 구독 모델 실제 호출은 이어집니다. 개발 DB에는 0019 마이그레이션이 필요합니다.
- 넓은 화면에서 접힌 사이드바는 접기 버튼에 포인터를 올리면 임시로 본문 위에 뜨고, 벗어나면 사라집니다. 클릭이나 Ctrl+B / Command+B로 고정해 연 상태는 본문을 옆으로 밉니다. 이 규칙은 `RULES.md` 8절이 정본입니다.
- PC에서 `http://127.0.0.1:3000`으로 열어도 로그인 Origin을 Neon이 기본으로 받는 `localhost`와 같게 봅니다. 휴대폰 Funnel Origin도 같은 localhost로 바꿉니다. 다른 사이트 Origin은 바꾸지 않습니다.
- 넓은 화면 사이드바는 왼쪽에서 슬라이드해 들어오고, 본문은 같이 옆으로 밀립니다. 접기 버튼은 하나이며 같은 자리에 있습니다. 이 규칙은 `RULES.md` 8절이 정본입니다.
- 넓은 화면의 접기 버튼을 하나로 모았습니다. 접히든 펼치든 화면 왼쪽 위 같은 자리에 있고, 본문은 옆으로만 밀리며 위아래로 움직이지 않습니다. 이 규칙은 `RULES.md` 8절이 정본입니다.
- 넓은 화면 사이드바를 더 좁히고, 열려 있는 동안 본문 위 차콜 탑바를 없앴습니다. CORELOOM 이름과 접기 버튼은 사이드바 안에만 두고, 접히면 본문 왼쪽 위에 펼치기 버튼만 남깁니다. Ctrl+B / Command+B는 그대로입니다. 이 규칙은 `RULES.md` 8절이 정본입니다.
- 넓은 화면의 좌측 내비게이션은 본문 위에 뜨지 않고, 열리면 본문을 옆으로 밀며 자리를 유지합니다. Windows는 Ctrl+B, Mac은 Command+B로 여닫고, 페이지를 옮겨도 연 상태를 기억합니다. 좁은 화면의 하단 메뉴바는 그대로입니다. 이 규칙은 `RULES.md` 8절이 정본입니다.
- 대시보드 오늘 할 일에서 설립 준비처럼 같은 주소로 가는 항목이 React 키를 겹치던 문제를 고쳤습니다. 브라우저가 보내는 Funnel Origin을 Neon Auth가 거부하지 않게, 인증 프록시가 그 Origin만 로컬 Next Origin으로 바꿉니다. 로그인 실패는 버튼이 풀리고 안내를 보여 줍니다.
- 접힌 폴드와 일반 휴대폰은 하단 메뉴바, 펼친 폴드와 데스크톱·4K는 좌측 드로어를 쓰도록 운영 화면 셸을 바꿨습니다. 사이드바는 고정하지 않으며, 메뉴를 연 뒤에만 본문을 가립니다. 이 규칙은 `RULES.md` 8절이 정본입니다.
- 개발 PC 동기화 제약을 정본에 적었습니다. 새 작업은 최신 `main`에서 새 브랜치를 따고, 머지된 옛 브랜치에 이어서 커밋하지 않습니다. 로컬은 `npm run up`과 HTTP `127.0.0.1:3000`만 쓰고, 휴대폰은 Tailscale 앱을 끈 채 HTTPS `:8443`만 엽니다. Funnel 주소·고객사명·이메일은 채팅·로그·커밋·PR에 넣지 않습니다. 스키마를 바꾸면 마이그레이션을 함께 두고, 개발 PC에서 `npm run db:migrate`가 필요한지 안내합니다. 개발 DB에는 2026-09-04에 0017·0018이 적용되어 있습니다.
- PC를 주기적으로 감시해 재기동하던 `npm run stay`는 쓰지 않습니다. 이 클라우드에서는 대표 PC를 대신 켤 수 없습니다.
- Windows에서 `npm run up`이 tsx CJS 변환 오류로 바로 실패하던 문제를 고쳤습니다. PC·Funnel 기동 동작은 그대로입니다.
- 휴대폰 Funnel을 `:8443`으로 고정했습니다. 같은 기기 호스트에서 다른 서비스와 섞이지 않게, 숫자 없는 `443`은 끄고 `10000`은 건드리지 않습니다. Funnel이 바깥으로 열 수 있는 포트는 `443`, `8443`, `10000`뿐입니다.
- 개발 PC에서 `npm run up`이면 `main` 최신을 받은 뒤 PC(`http://127.0.0.1:3000/dashboard`)와 휴대폰 Funnel을 같은 서버로 엽니다. `npm run dev`만 켜도 `main`이 깨끗하면 최신을 먼저 받습니다. 예전 3000번 서버는 끄고 다시 엽니다.
- 대표 화면의 색과 좌측 내비게이션을 크림·차콜·오렌지 셸로 바꿨습니다. 메뉴 그룹은 그대로 펼쳐 두고, 대시보드는 오늘 할 일·입금/지급 추이·프로젝트 단계 카드로 현황을 보게 했습니다. 조회만 하며 확정·발송은 각 화면에서 합니다.

## 2026-09-03

- 확정된 매출에 대해 환불을 등록할 수 있게 했습니다. 원래 금액을 덮어쓰지 않고 별도 환불 이력으로 남기며, 환불 합계는 원래 금액을 넘을 수 없습니다. 대표 승인이 필요합니다.
- `/admin/manual/progress`에서 시스템 구성 진행 현황을 추적합니다. 기능별 완료·PC 확인 필요·진행 중·미착수·자격 대기 상태를 표로 보여 주며, 기능이 추가되거나 상태가 바뀌면 같은 커밋에서 이 표를 갱신합니다.
- 설립 준비 항목을 증빙 위치 없이 완료로 바꿀 수 없게 막았습니다. 증빙을 먼저 적은 뒤에만 완료 상태로 저장할 수 있습니다.
- 대표 전용 AI 에이전트 시스템 계정과 업무 배정을 추가했습니다. 사람 계정과 다른 등록부에 이름·목적·허용 업무·접근 범위를 두고, 대표가 요청·입력·결과와 승인 여부를 이력으로 남깁니다. 에이전트는 금전·계약·권한·외부 공개를 혼자 실행하지 않으며, 로그인과 자동 실행은 포함하지 않습니다.
- 대표 전용 비공개 문서함에 원본 파일 업로드를 추가했습니다. 올린 파일은 이 PC의 비공개 폴더에 버전으로 보관하고 대표만 받을 수 있습니다. 기존 원본은 덮어쓰지 않으며 외부 공개는 하지 않습니다.
- 휴대폰 Funnel 주소(`기기.테일넷.ts.net`)에서 로그인 요청이 막히지 않도록 개발 허용 호스트를 고쳤습니다. 세션 쿠키는 같은 주소 이동에도 남도록 두었고, 로그인이 막히면 Neon Console Auth 신뢰 도메인에 그 HTTPS 주소를 넣도록 안내합니다.
- 대표 전용 `/admin/manual`을 추가했습니다. 저장소 `manual/` 원본을 읽기 전용으로 보여 주고, 배포 버전과 매뉴얼 원본 커밋을 함께 표시합니다. 화면에서 편집하지 않으며 화면용 사본을 만들지 않습니다.
- 로컬 Coreloom을 휴대폰에서 ALT. LIFE처럼 주소만으로 열 수 있게 했습니다. PC에서 Tailscale Funnel을 켜면 휴대폰에서는 Tailscale을 켜지 않고 HTTPS 웹앱으로 로그인합니다. 주소는 대표만 쓰고 다른 사람에게 보내지 않습니다.
- 대표 전용 비용 원장을 추가했습니다. 고객사 프로젝트 또는 앱·구독 사업에 비용을 연결하고, 연결하지 못한 건은 미분류로 표시합니다. 확정은 대표 승인 뒤에만 처리하며 자동 이체와 급여는 포함하지 않습니다. 대시보드에는 오늘 확인할 지급 예정과 비용 요약을 함께 보여 줍니다.
- 대표 전용 고객사 프로젝트 화면을 추가했습니다. 프로젝트마다 업무·견적·계약·청구·문서를 본문에 모으고, 오른쪽에서 Recho 근거와 AI 제안·확정 상태를 시간순으로 봅니다. Recho API 동기화는 포함하지 않습니다.
- 대표 전용 대시보드를 추가했습니다. 설립 준비 진행률과 오늘 확인할 견적·계약·청구·AI 제안을 먼저 보여 주고, 발송·체결·입금 확정은 각 화면에서 직접 하도록 두었습니다.
- 대표 전용 월 반복 청구 일정을 추가했습니다. 체결된 계약에 시작일·종료일·월 금액을 등록하면 최대 24개월의 예정 청구를 한 번에 만들고, 메일과 입금 확인은 각 청구에서 따로 합니다. 자동 발송과 세금계산서 발행은 포함하지 않습니다.
- 대표 전용 청구서 PDF·이메일 발송을 추가했습니다. 분할 청구 금액은 부가세 별도로 PDF를 만들고, 대표 승인 뒤에만 견적과 같은 Resend 연결로 발송합니다. 전자세금계산서 발행은 포함하지 않습니다.
- 대표 전용 비공개 문서함을 추가했습니다. 설립 증빙·계약·산출물·정산 자료의 원본 위치를 버전으로 보관하며, 파일 업로드와 외부 공개는 포함하지 않습니다.
- 대표 전용 매출 원장을 추가했습니다. 고객사 프로젝트 청구와 앱·구독 매출을 한 목록에서 보고, 연결하지 못한 건은 미분류로 표시합니다. 결제 채널 자동 수집과 환불은 포함하지 않습니다.
- 대표 전용 AI 제안·확정 이력을 추가했습니다. 근거 기록에 연결된 현재 합의·다음 할 일·위험 초안만 등록하며, 대표 확정 전에는 공식 결정으로 보이지 않습니다. 자동 생성과 외부 실행은 포함하지 않습니다.
- 대표 전용 Recho 근거 타임라인을 추가했습니다. 메일·통화·회의의 원문 식별자·발생 시각·연결 이유를 프로젝트에 연결해 시간순으로 보여 주며, 원문은 복제하거나 수정하지 않습니다. Recho API 동기화와 AI 제안은 포함하지 않습니다.
- 대표 전용 고객사 담당자 등록을 추가했습니다. 고객사에 이름·역할·이메일·전화·관계 상태를 연결해 보관하며, Recho 기록 연결은 포함하지 않습니다.
- 대표 전용 업무·일정 기능을 추가했습니다. 프로젝트에 업무를 연결하고 기한과 완료 조건을 기록하며, 진행 중인 업무는 기한별로 보여 줍니다. 완료는 대표 확인 뒤에만 처리하고 Recho 캘린더 연동은 포함하지 않습니다.
- 대표 전용 분할 청구 일정을 추가했습니다. 체결된 계약에 착수금·중도금·잔금을 등록하고, 청구일·입금 예정일·입금 확인을 함께 보관합니다. 입금 확정은 대표 승인 뒤에만 처리하며 청구서 메일과 세금계산서 발행은 포함하지 않습니다.
- 대표 전용 날인 계약 보관 기능을 추가했습니다. 견적 버전을 계약 초안으로 옮기고, 날인 원본 위치를 기록한 뒤 대표 승인으로 체결합니다. 체결된 버전은 덮어쓰지 않으며, 전자서명 공급자는 연결하지 않습니다.
- 개발 PC 전용 `/setup/email`에서 Resend API 키만 로컬 비밀 설정에 저장하는 연결 경로를 추가했습니다. 기본 테스트 발신 주소와 실제 고객사 발신 도메인의 전환 기준을 운영 매뉴얼에 기록했습니다.
- 견적서 PDF 첨부 이메일 발송 기반을 추가했습니다. 대표 승인 체크, 발송 요청·실패 이력, 중복 요청 방지 키를 적용했으며, Resend API 키와 인증된 발신 메일이 없으면 외부 발송을 차단합니다.
- 대표 전용 화면에 대시보드·회사 설립 준비·고객사·프로젝트·견적서로 이동하는 공통 좌측 내비게이션을 추가했습니다.
- 대표 전용 견적서 초안·수정 이력 기능을 추가했습니다. 견적은 고객사에 연결하고, 항목별 공급가액·부가세 별도(10%)·합계를 보관합니다. 수정은 새 버전으로만 저장되며 각 버전은 실제 PDF 파일을 내려받거나 인쇄할 수 있습니다.
- 프로젝트 목록에서 상태와 진행률을 수정하고 변경 이력을 남기는 기능을 추가했습니다.
- 대표 전용 고객사·프로젝트 등록 화면을 추가했습니다. 고객사에 프로젝트를 연결하고 상태와 진행률을 함께 관리합니다.
- 대표 전용 회사 설립 준비 화면을 추가했습니다. 개인사업자 등록 준비 항목별 상태·증빙 위치·확인 메모를 저장하고, 기본 항목 생성과 변경을 감사 이력에 남깁니다.
- 개발용 데이터베이스 연결 문자열을 로그인 설정과 분리해 로컬 전용으로 추가하는 경로를 만들었습니다. 이 경로는 기존 설정을 덮어쓰지 않고 `ai-development`만 대상으로 합니다.
- 개발 PC용 최초 연결 화면 `/setup`을 추가했습니다. 입력값은 새 `.env.local`에만 저장하며, 기존 파일과 운영 배포는 보호합니다.
- Coreloom의 첫 운영 매뉴얼을 만들었습니다.
- 미래 배포본의 관리자 전용 `/admin/manual`에서 저장소 `manual/` 원본을 읽기 전용으로 보여 주는 계약을 추가했습니다.
- 1인 창업자와 AI 에이전트 팀 운영 모델을 반영했습니다. AI 에이전트는 업무 초안과 요청까지만 수행하며, 금전·권한·계약·외부 공개의 최종 결정은 대표가 승인합니다.
- 회사 설립 준비, 고객사 견적·계약·분할 청구, Recho 근거 타임라인, 앱·구독 매출, 전자세금계산서 활성화 조건을 반영했습니다.
- 전용 Neon Free 프로젝트를 싱가포르 리전에 만들고, 운영 데이터 경계인 `production`과 AI 개발 전용 `ai-development` 브랜치를 분리했습니다. 운영 브랜치에는 AI MCP와 실험용 AI 도구를 연결하지 않습니다.
- 대표 전용 Neon Auth 로그인 화면과 서버 측 대표 이메일 검사 기반을 추가했습니다. 공개 가입은 사용하지 않으며, 최초 대표 계정과 비밀 환경값은 Console·로컬 환경에서만 관리합니다.
- 워크스페이스·구성원·감사 이력의 첫 Neon SQL 마이그레이션과 개발 브랜치 전용 적용 가드를 만들었습니다.
- 수동 운영 백업과 브랜치 승격 절차를 `docs/operations/neon-branch-and-backup-runbook.md`에 추가했습니다.
- 대표 전용 비밀번호 설정·재설정 화면을 추가했습니다. 요청 결과는 계정 존재 여부를 드러내지 않으며, 비밀번호는 대표가 메일 링크에서 직접 설정합니다.
