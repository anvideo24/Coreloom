# 고객사 → 견적 초안 실사용 검증

## 이번에 확인하는 것

견적을 작성하다 고객사를 새로 등록하고, 입력을 반복하지 않은 채 견적 초안을 저장할 수 있는지 확인한다. 닫기·새로고침·화면 전환 뒤의 초안도 비교한다. 실제 고객 정보·외부 발송·금액 확정은 사용하지 않는다.

환경은 개발 Windows PC의 Chrome 2560×1215다. Neon의 현재 브랜치·컴퓨트 정보와 로컬 연결 설정을 대조해 `ai-development`임을 확인했다. 실제 휴대폰 검증이나 사람의 작성 시간 측정이 아니다.

## 개선 전 — 실제 화면에서 발견한 실패

검증 코드: `b3d0587`.

| 동작 | 기대 | 실제 결과 |
| --- | --- | --- |
| 견적 입력 → 고객사 등록 화면 → 닫기 → 새 견적 | 제목·항목·단가·설명·메모 유지 | 제목·항목·단가 유지, 고객용 설명·메모 손실 |
| 인라인 고객사 상호 입력 → 닫기 → 새 견적 → 고객사 등록 | 입력한 상호 복원 | 빈 상호로 열림 |
| 인라인 고객사 저장 → 견적 복귀 | 새 고객사 자동 선택·이전 견적 유지 | 새 고객사는 선택 목록에 있으나 자동 선택되지 않음. 이전 견적 제목도 손실 |

마지막 실패에서는 잘못된 고객사로 저장하지 않도록 견적 저장을 멈췄다. 재검증까지 합쳐 개발 DB에 명확히 검증용으로 이름 붙인 가상 고객사 2건(A·B), B의 견적 초안 1건을 남겼다. 발송·계약 체결·매출 확정은 하지 않았다. 실제 고객 자료는 수정하지 않았다.

## 개선 후 체크리스트

검증 코드: `593174416eefd058bedbbce32e9c090e9c5fbc40`. 작업 가지에서 확인했으며 main 병합·CI 빌드 판정은 별도다.

| 검사 | 완료 기준 | 현재 결과 |
| --- | --- | --- |
| F01-01 | 같은 고객 정보 재입력 0회 | PC 한 사례 통과 · 고객명 재입력 0회. 담당자·프로젝트 없는 최소 사례 |
| F01-02 | 고객사 등록을 위한 외부 화면 왕복 0회·새 고객사 연결·견적 유지 | PC 한 사례 통과 · 외부 왕복 0회, 등록 직후 새 고객사 자동 선택·견적 입력 5/5 유지 |
| F01-04 | 저장·재조회한 고객사·품목·금액 일치 | PC 한 사례 통과 · 고객사·제목·품목·설명·메모·합계 6/6 일치. 저장 후 전체 새로고침으로 재확인 |
| F02-01 | 닫기·새로고침·다른 화면 복귀 후 초안 유지 | 부분 확인 · PC 견적 3/16조합, 각각 입력 5/5 보존. 인라인 상호 닫기·재열기도 별도 통과. 전체 통과 아님 |

### 무엇을 바꿨나

1. 제출용 숨은 값과 화면 입력이 분리된 견적 설명·메모를 복원 표식으로 저장한다.
2. 복원 이벤트가 일부만 복원된 상태를 다시 초안에 덮어쓰지 않게 한다. 닫기 후 재열기뿐 아니라 그 뒤 새로고침도 연속 확인했다.
3. 견적과 인라인 고객사 폼의 수명을 분리하고, 등록 후 URL로 지정된 새 고객사가 오래된 초안 선택보다 우선하도록 한다.

### 입력량과 화면 이동

| 항목 | 이번 사례 실측 |
| --- | --- |
| 직접 입력한 서로 다른 칸 | 6개 · 견적 주제, 작업명, 단가, 고객용 설명, 메모, 고객사 상호 |
| 한 번 작성하는 데 직접 입력한 글자 | 58자 · 공백 포함, 자동 천 단위 쉼표 제외 |
| 반복 고객명 입력 | 0회 |
| 고객사 등록을 위한 외부 화면 왕복 | 0회 |
| 정상 작성 경로의 내부 폼 전환 | 2회 · 견적→고객사 등록→견적 |
| 견적 목록에서 저장 상세까지 페이지 전환 | 1회 · 고객사 저장은 같은 견적 경로의 검색 인자만 바뀜 |
| 초안 내구성 확인용 추가 조작 | 닫기·재열기, 새로고침, 다른 페이지 왕복은 위 정상 경로 수와 분리 |
| 저장 후 새 견적에 이전 검증 입력이 되살아남 | 0개 |

거래 유형·발행일·유효기간·수량·역할은 기본값을 사용했다. 최소 필수 입력 수 전체를 주장하는 것이 아니라, 설명·메모 보존을 함께 검증한 이 사례에서 실제 입력한 수다.

### 자동 검사와 한계

- 설명·메모 복원을 추가한 회귀 시험이 수정 전 실패하고 수정 후 통과했다. 연속 재마운트 회귀도 포함했다.
- 관련 초안·수주 화면 시험 18개 통과, 전체 시험 470개(72개 파일)·타입 검사 통과.
- 변경 파일 lint는 기존 `set-state-in-effect` 3건 때문에 실패했다. 같은 파일의 수정 전 HEAD를 검사해 동일 3건임을 확인했다. 이번에 새 억제 규칙을 넣거나 인접 리팩터링으로 확장하지 않았다.
- 동적 패키지 추가·삭제 수 자체의 복원은 이번 한 품목 사례에서 검증하지 않았다. 전체 견적 입력 보존 완료로 읽지 않는다.

## 다품목 후속 — 추가·삭제 후 복원

2026-09-06, 앞선 변경이 PR #113으로 main에 병합된 `1e2adc1`에서 개발 PC Chrome 2560×1215로 확인했다. 이번 시험은 브라우저의 가상 초안만 사용하며 DB 저장·외부 발송은 하지 않는다.

수정 후 검증 코드: `5e429d087c8db6643914756ba1f33c637b7335e5`. 아래 결과는 이 코드의 PC·자동 시험이다. 외부 CI·main 병합의 현재 상태는 [PR #114](https://github.com/anvideo24/Coreloom/pull/114)에서 별도로 확인한다.

| ID | 재현·완료 기준 | 수정 전 실제 결과 | 수정 후 |
| --- | --- | --- | --- |
| QD-01 | A·B·C 3품목 입력 → 닫기·재열기, 수·순서·작업명·단가 유지 | 실패 · A 1품목만 남음 | 통과 · PC 3품목 유지, 이름·단가·설명 9/9 일치 |
| QD-02 | A·B·C 중 B 삭제 → 새로고침, A·C 2품목 유지 | 실패 · A 1품목만 남음 | 통과 · PC A·C 유지, 이름·단가·설명 6/6 일치 |
| QD-03 | 첫 품목 삭제 → 재열기·새로고침, 지운 품목 되살아남 0개 | 미측정 | 통과 · PC C만 유지, 지운 A·B 되살아남 0개 |
| QD-04 | 빈 품목 추가 직후 재열기, 추가한 개수 유지 | 미측정 | 통과 · PC C 다음 빈 품목 유지, 2개 일치 |
| QD-05 | 품목별 입력·계산 설정을 두 번 연속 복원, 제출 값 일치 | 미측정 | 통과 · 자동 시험에서 역할·단가·개월·인원·가동률·수량·금액·잠금·설명 비교. PC는 19개 표시값이 재열기 및 연속 새로고침에서 각각 19/19 일치, 수동 금액 잠금 1개 유지 |
| QD-06 | 검증 초안 버리기 후 재열기, 가상 입력 잔존 0개 | 이전 검증 정리 시 0개 | 통과 · PC 잔존 0개, 기본 빈 품목 1개·기본 마진/운영비 복귀 |

원인: 개별 입력칸 복원과 달리 품목 배열을 편집기 상태로 되돌리는 경로가 없었다. 제출용 `packagesJson`은 숨은 값이라 일반 입력 복원에서 제외되고, 새 편집기는 기본 1품목으로 시작했다. 품목 배열의 복원과 추가·삭제 직후 저장을 함께 수정한다. 단순히 기본 품목 수를 늘리는 방식은 쓰지 않는다.

후속 검증 중 마진·운영비가 새로고침에서 기본값으로 돌아가는 실패도 확인했다. 슬라이더 표시만 복원하는 중간 구현 대신 제출용 값에서 계산 상태를 복원하도록 보완한 뒤, 새로 연 브라우저에서 마진·운영비 각 1%, 수동 공급가·자동 계산 금액이 재열기와 새로고침 모두 유지됨을 확인했다. 최종 19개 값 비교에는 작업명·역할·단가·개월·인원·가동률·수량·공급가·펼친 설명과 마진·운영비가 포함된다. 시험용 C와 빈 품목 두 개의 당시 표시값을 그대로 비교했으며 접힌 빈 설명을 검사 수에 넣지 않았다.

자동 시험은 수정 전 4품목 기대/1품목 실제 실패를 확인한 뒤 통과시켰다. 내부 편집 탭의 비기본 마진·운영비 및 수동 금액 잠금, 두 번 연속 복원, 손상 JSON 안전 처리 회귀도 추가했다. 품목 JSON 초안은 64,000자까지 저장하고 다른 단일 필드의 8,000자 한도는 유지한다. 저장 한도 초과·실물 Fold·서버 저장 성공/실패의 전체 조합은 이번 PC 시험으로 통과 처리하지 않는다.

최종 검사: 관련 4파일 33개 시험, 전체 72파일 473개 시험, 타입 검사 통과. 수정 전부터 있던 `draft-aware-form.tsx`의 `set-state-in-effect` lint 오류 1건은 남아 있다. lint 전체 통과나 main 반영을 주장하지 않는다.

이 체크리스트는 F02의 PC 견적 초안 보존을 더 깊게 확인하는 하위 사례다. 사례 수를 전체 16조합의 추가 통과로 합산하지 않으며 실제 Fold·저장 실패 등 나머지 조합은 별도다.

## 저장 실패 후 재시도 — 자동 검증

이 절은 수정 전 `26ef77a`의 기록이다. 후속 수정 결과는 아래 「저장 요청 보존 보완」에서 별도로 본다.

2026-09-06 검증 대상 제품 코드: `26ef77a0ca072073c056caebc81531e55ae25623`. 제품 코드는 바꾸지 않고 `form-draft-scenarios.test.tsx`에 다품목 실패·재제출 시험만 추가했다. Vitest/jsdom에서 실제 폼 컴포넌트와 시험용 실패 action을 사용했다. 실제 서버 장애·DB 저장·브라우저·휴대폰을 시험한 것은 아니다. 실제 기기 시험은 대표 요청에 따라 모아 나중에 진행한다.

| ID | 확인할 것 | 결과 |
| --- | --- | --- |
| QR-01 | 저장 action 실패 뒤 임시 초안 유지 | 통과 · 실패 1회 후 초안 있음 |
| QR-02 | 다시 열어 두 품목·설명·마진·운영비·수동 금액 복원 | 통과 · 전체 품목 JSON과 서버 action이 읽는 제출 필드를 실패 전과 대조해 일치 |
| QR-03 | 복원 후 재제출 가능, 성공 후 임시 초안 제거 | 통과 · 재제출 1회 호출, 성공 후 초안 없음 |
| QR-04 | 실제 오류 화면에서 재시도 안내·조작 가능 | 미검증 · 단위 시험에서는 시험용 오류 경계 뒤 폼을 다시 열었음. 사용자용 복구 버튼 확인이 아님 |
| QR-05 | 저장 성공·응답 유실 후 다시 열어 재시도해도 중복 0건 | 위험 확인 · 재열기에 제출 식별자가 달라짐. 실제 DB 중복 발생은 시험하지 않았으며 통과 아님 |

관련 시험 2파일 28개 통과. 기존 서버 중복 방지 시험은 **같은 제출 식별자**를 다시 보냈을 때의 동작이다. `DraftAwareForm`은 재마운트 때 식별자를 새로 만들고, 서버는 그 식별자를 기준으로 중복을 가른다. 따라서 입력 보존 통과를 응답 유실 재시도의 중복 방지 통과로 확대하지 않는다. 이 턴에서는 수정하지 않고 후속 승인 대상으로 남긴다.

## 저장 요청 보존 보완

2026-09-06 작업 가지 `codex/quote-retry-identity`, 검증 코드 `6f63026c46d4e67b43e18aac2ceac5cec95a4980`에서 새 견적만 수정했다. 먼저 QR-05의 동일 식별자 기대 시험이 실패하는 것을 확인한 뒤 보완했다. 자동 시험은 실제 `DraftAwareForm`·견적 편집기를 사용하고 서버 저장/응답만 시험 대역으로 바꾼다. 재마운트는 같은 탭의 재열기·새로고침 수명 주기를 모사하며 실제 브라우저 새로고침 측정은 아니다.

| ID | 완료 기준 | 수정 후 자동 결과 |
| --- | --- | --- |
| QR-05 | 저장 완료 후 응답 유실 → 재열기 → 같은 내용 재시도, 저장 결과 1건 | 통과 · 요청 UUID·서버 제출 필드 일치, 가짜 저장 1건. 내부 원가에서 고객용 미리보기 전환도 포함 |
| QR-06 | 미확정 요청의 내용을 변경한 재시도는 조용히 기존 결과로 처리하지 않음 | 통과 · action 호출 0회, 안내 표시, 바뀐 입력 유지 |
| QR-07 | 요청 정보를 저장하지 못하면 전송하지 않고 입력 유지 | 통과 · 저장소 쓰기 실패 시 action 0회, 입력 유지 |
| QR-08 | 다른 사용자 스코프의 요청은 섞이지 않음 | 통과 · 스코프 2개에서 다른 UUID |
| QR-09 | 성공 후 새 제출은 새 요청으로 시작 | 통과 · 두 제출 UUID가 서로 다름 |
| QR-10 | 명시적 초안 버리기 후 새 제출은 옛 요청을 사용하지 않음 | 통과 · 실패 요청과 다른 UUID |
| QR-11 | 전송 준비 중 초안 버리기 후 늦은 처리로 요청이 되살아나지 않음 | 통과 · 요청 정보 0개·action 0회, 이후 새 제출 가능 |
| QR-12 | 전송 준비 중 폼 닫기 후 늦은 처리가 전송하지 않음 | 통과 · 요청 정보 0개·action 0회, 재열기 뒤 제출 가능 |

최종 로컬 검사: 전체 **72파일 481개 시험 통과**, TypeScript 검사 통과, diff 공백 검사 통과. 집중 시나리오는 22개 통과다. 관련 lint는 기존 `quotes-page-client.tsx`의 effect 안 상태 변경 오류 2건이 남아 전체 통과로 보고하지 않는다. 실행 중 개발 서버 보호를 위해 이번 로컬 빌드·재기동은 하지 않았다. 외부 CI·main 병합의 현재 상태는 [PR #115](https://github.com/anvideo24/Coreloom/pull/115)에서 별도로 확인한다.

실제 서버 장애 주입·DB 결과 건수 대조·실제 오류 화면(QR-04)·Fold 검증은 하지 않았다. 서버의 기존 workspace+submissionId 중복 조회 경로는 유지하며, DB 구조나 저장 절차는 바꾸지 않는다. F02-03 전체 판정은 이 모의 시험만으로 통과 처리하지 않는다. 사람·실기기 시험은 대표 요청대로 추후 모아서 진행한다.

## 저장 오류 복구 UI — 신규 견적

2026-09-06, `codex/quote-save-recovery`의 검증 코드 `47c132d616dcd5e4eccd90e2a1b074347b12eacd`에서 QR-04의 사용자용 복구 경로를 추가했다. 수정 전 자동 시험에서는 action 오류가 작성창을 오류 경계 화면으로 바꾸어 복구 안내를 찾을 수 없었다. 수정 후에는 새 견적만 작성창 안에서 복구하며 다른 폼의 오류 전파는 유지한다.

| ID | 완료 기준 | 자동 시험 결과 |
| --- | --- | --- |
| QR-13 | 실패 뒤 같은 작성창에서 입력·초안 유지 | 통과 · 제어 입력인 제목과 비제어 입력인 프로젝트 선택 유지, 초안·요청 정보 보존 |
| QR-14 | 명시적 재시도는 같은 요청으로 실행 | 통과 · 실패→재시도 2회 호출, UUID·제출 필드 일치, 성공 후 초안·요청 정보 제거 |
| QR-15 | 전송 전 차단과 결과 불확실을 구분 | 통과 · 저장소 쓰기 실패는 action 0회와 전송 전 안내, action 오류는 저장 여부 확인 안내 |
| QR-16 | 미확정 요청 재열기와 목록 확인 경로 제공 | 통과 · 재마운트 직후 확인 안내·재시도·`/quotes` 링크 존재. 실제 링크 이동·브라우저 복귀는 미검증 |
| QR-17 | 내용이 달라진 재전송은 차단하고 판단 경로 제공 | 통과 · action 0회, 수정값 유지, 재시도 버튼 없이 목록 확인 링크 제공 |
| QR-18 | 저장 중 연타·초안 삭제 방지 | 통과 · 저장 버튼 비활성, 연속 클릭에 action 1회, 처리 중 초안 버리기 숨김, 완료 뒤 저장 정보 정리 |
| QR-19 | 성공 리다이렉트를 실패로 삼키지 않음 | 통과 · 리다이렉트 오류 재전파와 초안·요청 정보 제거, 복구 안내 없음 |

최종 로컬 검사: 집중 시나리오 26개, 전체 **72파일 485개 시험 통과**, TypeScript·diff 공백 검사 통과. 개발 서버 보호를 위해 로컬 빌드·재기동은 하지 않았다. 처음 시도한 오류 후 reset 방지는 비제어 입력 초기화 시험에서 실패해 폐기하고, 신규 견적의 검증된 제출을 transition에서 실행하는 방식으로 변경했다.

오류 안내로 초점이 이동함은 자동 시험으로 확인했다. 안내와 버튼은 기존 디자인 토큰을 사용하고 한글 단어 단위 줄바꿈·44px 이상 조작 영역·좁은 화면 세로 배치를 지정했다. 실제 4K·Fold 화면에서의 가독성과 스크롤 위치까지 확인한 것은 아니다.

실제 서버 장애·DB 저장 결과·브라우저 오류 화면(QR-04)·휴대폰은 미검증이다. 이 결과는 Vitest/jsdom의 실제 폼 컴포넌트와 시험용 action을 사용한 자동 검사이며 F02 전체 완료로 올리지 않는다. DB·환경 설정·개발 서버는 건드리지 않았고 실제 기기 시험은 추후 일괄 진행한다. 외부 CI·main 병합의 현재 상태는 [PR #116](https://github.com/anvideo24/Coreloom/pull/116)에서 별도로 확인한다.

## 측정 범위와 남은 것

- 직접 입력한 필드 수와 중복 입력, 페이지 이동·패널 전환을 구분해 기록한다.
- 네트워크 대기·AI 도구 호출 시간은 사람의 작성 시간으로 계산하지 않는다.
- F01-03 입력량 30% 감소는 같은 조건의 개선 전 완료 흐름과 비교해야 한다. 개선 전 흐름이 손실로 중단되어 이번 한 사례만으로 감소율을 확정하지 않는다.
- 사람 5사례 작성 시간 및 실제 Fold 검증은 별도로 남긴다.
