# 작성·검토·매뉴얼 이어 쓰기

## 이번 목표

업무·비용 작성 중 입력을 잃지 않고, 승인 상세에서 돌아와 탐색을 이어 가며, 매뉴얼에서 현재 안내와 과거 기록을 구별한다. 이 세 범위만 개선한다. 자동 시험과 실제 PC·4K·Fold 확인은 별도로 기록한다.

| ID | 목표 | 확인 방법 | 상태 |
| --- | --- | --- | --- |
| OC-01 | 업무·비용 두 작성창 모두 제목·명시적 닫기 제공 | 실제 컴포넌트 렌더링, 두 닫기 동작 검사 | ☑ 자동 시험 2/2 |
| OC-02 | 두 폼 × 닫기·새로고침·다른 화면 복귀 6조합 입력 일치 | 탭 저장소를 유지한 닫기·재마운트 시험; 실제 기기는 별도 | ☑ 자동 시험 6/6 |
| OC-03 | 업무 유형과 해당 연결값 복원, 다른 유형의 연결 제출 0건 | 회사·자체 사업·고객사 유형 및 변경 후 제출값 대조 | ☑ 자동 시험 · 3유형과 전환 제출 |
| OC-04 | 저장 성공·명시적 버리기 뒤 초안 잔존 0건, 실패 뒤 입력 보존 | 가상 저장 성공·실패 및 버리기 취소/확인 시험 | ☑ 두 폼 자동 시험 통과 |
| OC-05 | 다른 사용자·다른 폼의 초안 혼입 0건 | 서로 다른 범위로 렌더링·저장소 대조 | ☑ 자동 시험 · 사용자 변경·교차 폼 복귀 |
| OC-06 | 승인 목록 복귀 5사례에서 검색·분류·검토 항목 유지 | 검색만·분류만·결합 후 항목 스크롤·삭제된 항목·결과 없음 | ☑ UI 자동 시험 5/5 · 실제 브라우저 복귀는 별도 |
| OC-07 | 로그아웃 성공 뒤 승인 탐색 기록 제거, 실패 시 유지 | 인증 응답 대역을 통한 실제 로그아웃 버튼 시험 | ☑ 자동 시험 · 실패→재시도 성공 |
| OC-08 | 현재 안내가 과거 기록보다 먼저, 과거 기록은 펼쳐 읽기 | 실제 매뉴얼 원본 파싱·렌더링 검사 | ☑ 자동 시험 · 현재 요약 가시성·초기 접힘 |
| OC-09 | 과거 본문 손실 0건, 제목 링크로 숨겨진 내용 접근 가능 | 변경 전 원본과 비교, 앵커·펼치기 검사 | ☑ 진행 현황 기존 비어 있지 않은 줄 누락 0 · hash 펼침/스크롤 자동 시험 |
| OC-10 | PC·4K·폴드에서 닫기·복귀·현재/과거 구별 가능 | 누적 실사용 시험 때 별도 측정 | 미검증 · 추후 일괄 확인 |

## 확인할 때의 경계

자동 시험 근거: `apps/web/tests/task-expense-form-drafts.test.tsx`(18개), `approvals-page-client.test.tsx`·`approval-navigation.test.ts`(합계 16개), `sign-out.test.tsx`(6개), `admin-manual.test.ts`·`src/components/admin-manual-frame.test.tsx`(합계 22개). 2026-09-06 확인이며 이 수는 각 파일의 기존 시험도 포함한다. 실제 DB·서버·PC·Fold 시험을 뜻하지 않는다.

통합 확인: 2026-09-06 14:57 KST 전체 79파일 587개 시험·타입 검사 통과. 진행 현황의 기존 비어 있지 않은 줄 156/156개가 순서대로 보존됐다. 대상 코드 검사에는 기존 업무·비용 파일의 effect 상태 변경 2건·내부 링크 3건 오류가 남아 있으므로 전체 코드 검사 통과라고 표시하지 않는다. 이 변경으로 해당 기존 줄은 수정하지 않았다.

구현은 `codex/ux-drafts-return-manual` 작업 가지다. main 병합과 사용 환경 반영은 별도로 확인한다. 원문 보존 대조 기준은 직전 main `ad48440`이다.

- 모의 새로고침·화면 복귀는 컴포넌트를 다시 올리는 자동 시험이다. 실제 브라우저나 실제 DB 저장 확인으로 표시하지 않는다.
- 초안과 승인함 탐색 기록은 같은 탭의 임시 데이터다. 다른 기기 동기화나 업무 확정 기록이 아니다.
- 실제 회사·고객 자료 대신 가상 자료를 사용한다. 저장·승인·발송·DB 변경을 이 문서만으로 실행하지 않는다.
- 기존 개선 목표 28개와 결과 이력은 변경하지 않는다. 이번 검사는 그 통과 수에 자동 합산하지 않는다.
