# 승인 상세 확인 기록

통합 상태는 [PR #117](https://github.com/anvideo24/Coreloom/pull/117)에서 확인한다. 승인함과 합친 코드 `7be4d57`의 전체 74파일 490개 시험·타입 검사 통과. 아래 개별 검사 이력과 미측정 실제 화면·대표 판단 항목을 그대로 보존하며 CI·main 병합이 실기기 검증을 대신하지 않는다.

## 목표와 범위

대상·금액을 먼저 읽고, 기록된 증빙/참고와 확정 후 결과를 구분한다. 빠른 승인 자체가 아니라 잘못된 대상·근거로 결정하지 않는 것이 목표다.

- 대상: 비용·매출·입금·계약·AI 제안·에이전트 작업 상세 여섯 종류.
- 보존: 실제 승인·반려 조건, 대표 인증, 필수 체크·반려 사유·결과 입력, 확정된 기록 보호.
- 제외: DB·마이그레이션·서버 재기동·실제 승인/발송, 환불 흐름 변경, 외부 CI·main 병합.
- 작업 가지: `codex/approval-detail-clarity`. 이전 승인함 작업은 `codex/approval-inbox-clarity`에 별도 보존한다.
- 검증 코드: `084003da44040684f997131b7f030f1929e3fb51`. 아래 결과는 이 작업 가지의 로컬 검사이며 main 병합·실기기 검증은 아니다.

## 자동 검사·코드 대조

| 완료 | ID | 목표·통과 기준 | 확인 방법 | 결과 |
| --- | --- | --- | --- | --- |
| ☑ | AD-01 | 대상·금액·참고·확정 후 결과 4구역 표시 | 실제 공통 컴포넌트 렌더링 | 통과 · 4개 라벨의 표시·순서 일치 |
| ☑ | AD-02 | 0원과 금액 없음 혼동 0건 | 0원·빈 금액 가상 사례 렌더링 | 통과 · 2/2 표시값 일치. 실제 서버 금액 대조는 별도 |
| ☑ | AD-03 | 참고 정보 존재를 원본 검증 완료로 표시 0건 | 기록값 표시·비검증 안내 확인, 페이지 연결 코드 대조 | 통과 · 경로/없음 표시 유지, 자동 검증 결과가 아님을 안내 |
| ☑ | AD-04 | 가상 한글·경로·HTML 모양 입력의 텍스트 손실/실행 0건 | 원문 텍스트 유지·이스케이프 검사 | 통과 · 제목·가상 경로 일치, HTML 모양 문자열은 텍스트이며 이미지 요소 0개. 실제 긴 글 배치는 AD-07 |
| ☑ | AD-05 | 여섯 종류 승인함 복귀·확정 직전 확인 영역 제공 | 상세 페이지 연결 및 기존 폼 코드 대조 | 코드 확인 · 6/6 연결. 실제 여섯 페이지 렌더·이동 시험은 미실행 |
| ☑ | AD-06 | 기존 필수 승인 체크·반려/결과 입력과 확정 보호 유지 | 변경 비교·관련 도메인 회귀 시험 | 통과 · 기존 폼 필드·액션·인증·상태 조건 변경 없음. 결과 미입력 시 필수 입력 유지, 이력 승인 후 결과 안내는 항상 표시. 실제 제출 미실행 |

2026-09-06 Windows 개발 PC에서 공통 카드 새 시험 2개가 기존 제목·비검증 안내 누락으로 실패함을 확인한 뒤 수정했다. 최종 전체 **73파일 487개 시험**, TypeScript, 변경 TSX 파일 ESLint, diff 공백 검사 통과. 자동 시험 원본은 `apps/web/tests/approval-review-card.test.tsx`, 기존 확정 보호 회귀 시험은 `apps/web/tests/approval-gates.test.ts` 등이다. 실제 페이지별 서버 데이터/폼 동작을 대역으로 재현한 시험은 추가하지 않았다.

대상·금액의 글자 크기·굵기를 높이고 참고 정보는 보조 위계로, 확정 후 결과만 얕은 배경으로 구분했다. 기존 디자인 토큰·단어 단위 줄바꿈·긴 문자열 비상 줄바꿈을 사용하며 640px 이하에서 한 열로 읽는다. 사용자 요청의 실제 4K·Fold 확인은 아직 하지 않았다.

자동 렌더링 검사는 실제 서버 저장·CSS 배치·사용자 이해도 시험을 대신하지 않는다. 불러온 기록만 보여 주며 원본 문서의 진위나 입금 사실을 자동 검증하는 기능이 아니다.

## 후속 자동 시험 — 실제 페이지 연결

기존 AD-05·06의 코드 대조를 보완하기 위해 여섯 상세 페이지 함수를 직접 호출하고 반환된 화면을 jsdom에서 렌더링한다. 인증·자료 조회·서버 액션과 대화 등 범위 밖 하위 화면만 대역으로 바꾸며, 확인 카드와 요약 계산은 실제 코드를 사용한다. 모든 자료는 가상 값이다.

대상 제품 코드 기준은 `d0252ce`이며 제품 코드는 수정하지 않는다. 자동 시험 원본은 `apps/web/tests/approval-detail-pages.test.tsx`다. 이 검사는 CSS 배치·실제 로그인·HTTP 이동·서버 제출·DB 저장을 검증하지 않는다. 기존 AD-01~09 이력과 F03 판정은 덮어쓰지 않는다.

| 완료 | ID | 목표·통과 기준 | 결과 |
| --- | --- | --- | --- |
| ☑ | AD-10 | 6종 실제 페이지의 대상·금액·참고·결과 및 승인함 링크 일치 | 통과 · 카드 내부 24개 값과 복귀 링크 6개 일치. 금전 4종은 가상 0원, 제안/작업은 금액 없음 |
| ☑ | AD-11 | 미로그인·권한 없음 2상태 × 6종에서 조회 전 차단 | 통과 · 12/12 리다이렉트 조건, 자료 조회 호출 0회. 실제 로그인 시험 아님 |
| ☑ | AD-12 | 없는 기록 6종에서 찾을 수 없음 처리 | 통과 · 6/6 notFound 호출을 예외 대역으로 확인. 실제 HTTP 404 시험 아님 |
| ☑ | AD-13 | 대기 상태의 필수 승인 입력과 확정 후 재승인 방지 유지 | 통과 · 6종 필수 approved 입력 및 대상 식별자, 결정 후 승인 버튼 제거. 서버 제출·확정 보호 자체는 이 시험 밖 |
| ☑ | AD-14 | 원본 없는 계약·금액 없는 제안/작업·반려/결과 입력 경계 유지 | 통과 · 원본 미등록 초안의 체결 버튼 없음, 제안/작업 금액 없음, 반려 사유·작업 결과 required 유지 |

2026-09-06 Windows 개발 PC에서 신규 **22개 시험**, 전체 **75파일 512개 시험** 통과. 인증·자료 조회·서버 액션은 대역이며 각 시험 종료 시 모든 서버 액션 대역의 호출 0회를 확인한다. 위 결과는 페이지가 반환한 HTML의 조건이며 체크박스 미선택 제출·실제 서버 요청 차단이나 대표 판단 완료를 뜻하지 않는다. 실행은 `apps/web`에서 `npm test -- --run tests/approval-detail-pages.test.tsx`, 전체 검사는 `npm test`다. 외부 CI·main 반영은 별도 확인한다.

## 후속 자동 시험 — 승인·반려 서버 처리 함수

페이지 렌더링과 별개로 비용·매출·입금 확정, 계약 체결, AI 제안 확정/반려, 에이전트 이력 승인/반려의 **8개 서버 액션**을 가상 FormData로 직접 호출한다. 실제 액션·저장 처리 함수·도메인 판단을 연결하고, 인증 세션·워크스페이스 준비·DB와 외부 입출력만 대역으로 격리한다. 제품 코드 기준은 `a215201`이다.

| 완료 | ID | 목표·통과 기준 | 결과 |
| --- | --- | --- | --- |
| ☑ | AD-15 | 미로그인·권한 없음 2상태 × 8액션에서 DB 접근 전 차단 | 통과 · 16/16. 워크스페이스 준비·DB 생성/조회·쓰기·갱신/이동 호출 0회 |
| ☑ | AD-16 | 정확한 승인값이 없는 8액션은 업무 기록·감사 이력 쓰기 0회 | 통과 · 누락/false/on/파일 4조건 × 8 = 32/32. 승인 필요 오류 일치, 쓰기·성공 후 갱신/이동 0회 |
| ☑ | AD-17 | 이미 결정된 기록 8액션에서 재확정·재반려 쓰기 0회 | 통과 · 8/8. 결정 상태 오류 일치, 쓰기·성공 후 갱신/이동 0회. 동시 요청 시험 아님 |
| ☑ | AD-18 | 없는 기록 8액션에서 쓰기 0회 | 통과 · 8/8. 대상 없음 오류 일치, 쓰기·성공 후 갱신/이동 0회 |
| ☑ | AD-19 | 빈 반려 사유·작업 결과·계약 원본의 필수 조건 누락 시 쓰기 0회 | 통과 · 4/4. 반려 사유 2종, 작업 결과, 계약 원본의 해당 오류 일치·쓰기 0회 |
| ☑ | AD-20 | 유효 요청 8액션에서만 가상 저장과 성공 후 이동 발생 | 통과 · 8/8. 예상 수정/이력 삽입 횟수·이동 경로 및 감사 기록의 작업공간/실행자/사건/대상 일치. 폼의 위조 실행자 대신 세션 실행자 사용 |

2026-09-06 Windows 개발 PC에서 `apps/web/tests/approval-actions.test.ts` 신규 **33개 시험(위 76조건)**, 전체 **76파일 545개 시험**, TypeScript·추가 파일 ESLint 검사 통과. `apps/web`에서 `npm test -- --run tests/approval-actions.test.ts`로 재실행한다. 도메인 판단을 테스트용 복제품으로 바꾸지 않고 실제 처리 함수를 검사했다. 인증 차단 이외에는 가상 기록을 먼저 읽을 수 있으나 거절된 업무 기록·감사 이력 쓰기는 0회다. 외부 CI·main 반영은 별도 확인한다.

쓰기 0회는 격리한 DB 대역의 호출 횟수다. 실제 DB 원자성·동시 요청·워크스페이스 소유권 SQL·HTTP 인증/Origin·실기기·사용자 판단은 이 시험의 완료 주장에 포함하지 않는다. F03 판정과 이전 이력은 그대로 보존한다.

## 나중에 모아서 할 실제 시험

| 완료 | ID | 목표·통과 기준 | 확인 방법 | 결과 |
| --- | --- | --- | --- | --- |
| ☐ | AD-07 | PC 일반·4K·Fold 닫힘/펼침 4환경에서 잘림·겹침·페이지 가로 넘침 0건 | 긴 한글·긴 경로·0원/금액 없는 화면을 실제 기기에서 확인 | 미검증 · 추후 일괄 |
| ☐ | AD-08 | 고정 정답 5사례에서 대상·금액·증빙·확정 결과 20/20 일치 | F03-03 확정 직전 실제 화면 대조. 실제 확정 실행하지 않음 | 미검증 · 추후 일괄 |
| ☐ | AD-09 | 대표 판단 5사례 오판 0건 | F03-02에 따라 답과 이유 확인, 판단 시간 전후 기록 | 미측정 · 대표 확인 |

F03의 통과 여부는 기존 계획과 결과 파일에서 판단한다. AD 자동 항목 통과만으로 실제 5사례·사람 판단·실기기 항목을 완료 처리하지 않는다.
