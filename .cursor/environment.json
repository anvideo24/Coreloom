{
  "name": "Coreloom web (apps/web)",
  "user": "ubuntu",
  "install": "cd apps/web && npm ci",
  "terminals": [
    {
      "name": "web-dev",
      "command": "cd apps/web && npx next dev --hostname 127.0.0.1 --port 3000",
      "description": "Coreloom Next.js 개발 서버. http://127.0.0.1:3000 에서 열립니다. /setup, /sign-in 은 DB 없이 열리고, /dashboard 등 운영 화면은 DATABASE_URL 과 Neon Auth 설정이 있어야 동작합니다."
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 3000
    }
  ]
}
