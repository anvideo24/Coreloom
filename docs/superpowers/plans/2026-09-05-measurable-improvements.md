# 코어룸 개선 목표·검증 계획

계획 버전: 1 · 작성일: 2026-09-05 · 상태: 1단계 초안 준비 / 기준 측정 일부 완료

> 지금 보는 것은 계획 원본입니다. 제품 기능 수정과 관리 화면 구현은 아직 시작하지 않았습니다.

## 지금 볼 것

| 확인 대상 | 상태 | 다음 행동 |
|---|---|---|
| 공용 규칙 변경안 | 검토용 초안 | working-method의 proposals/measurable-feature-verification.md 검토 |
| 기능별 목표·체크리스트 | 아래 F01–F07 | 목표값과 측정 범위 확정 |
| 개선 전 증거 | [기준 측정 기록](../../quality/baseline-2026-09-05.md) | 미측정 항목 보완 |
| 제품 코드 개선 | 미착수 | 후속 단계 승인 후 Sonnet에 위임 |
| 앱 안의 관리 화면 | 미구현 | F07 검토 후 구현 |

공용 형식과 판정 방식은 working-method에서 관리한다. 이 문서는 코어룸의 목표·검사 항목 원본이며 공용 규칙 사본이 아니다. 1단계 승인은 계획·측정 준비 승인이다. 아래 목표 수치는 구현 전에 최종 고정하며, 모든 제품에 적용되는 수치는 아니다.

## 범위와 역할

- 이번 단계: 공용 변경안, 제품별 검사 목록, 비파괴 기준 측정, 사용자 열람용 문서.
- 이번 단계 제외: 제품 코드·DB·설정 변경, 실제 업무 데이터 생성, 외부 발송, PR·배포, 전체 제품 일괄 변경.
- 지휘·리뷰·검증: Codex. 후속 실제 코딩: Claude Sonnet 한 작업 묶음씩. 사용자: 목표 승인과 사람의 판단·이해도 측정.
- Sonnet 구독 실행 시 실제 모델을 확인한다. 다른 모델·유료 API로 임의 대체하지 않는다. 이번 단계에는 실제 코딩이 없어 모델을 호출하지 않았다.
- 한 파일의 공동 변경을 보존하고 맡은 파일만 커밋한다. 제품 코드·제품 규칙·매뉴얼은 같은 변경으로 관리한다. 공용 저장소의 변경은 별도 커밋으로 연결한다.

## 공통 측정 조건 — 코어룸

1. 데이터는 UX 전용 가상 사례만 사용한다. 사람 판단 시험의 정답표는 시험 전에 고정하며, 수행자에게 미리 보여주지 않는다.
2. PC와 실제 휴대폰을 구별한다. 브라우저 폭 재현은 모바일 레이아웃 측정일 뿐 실제 기기 시험을 대체하지 않는다. 기기·브라우저·뷰포트·계획 버전·검증 코드 버전을 기록한다.
3. 사람 시간은 연습 1회 제외 후 동등 난이도 사례 5회로 중앙값·최소·최대·정확도를 남긴다. 네트워크 대기와 실제 조작 시간을 별도 기록한다. 전후 학습 영향과 작은 표본 한계를 표시한다.
4. 입력량은 사용자가 직접 값을 넣은 필드 수, 반복 입력 필드 수, 직접 입력 문자 수를 나눠 기록한다. 화면에 보이는 필드 수를 직접 입력량으로 대신하지 않는다.
5. 경로 전환은 페이지 이동·패널 개폐·탭 전환을 각각 센다. 왕복은 현재 작업을 떠나 선행 정보를 등록한 뒤 돌아오는 구간이다. 뒤로 돌아온 값이 유지되는지도 확인한다.
6. 미측정은 숫자 0이 아니다. 원인, 필요한 준비, 재개 조건을 함께 적는다. 사용자 측정은 AI가 대신 통과 처리하지 않는다.

## F01 · 고객사에서 견적 초안까지

목표: 같은 정보를 다시 쓰거나 고객사 등록 때문에 견적 작업을 잃지 않는다.

범위: 최소 고객사 생성, 견적 안에서 고객사 추가와 복귀, 고객사·프로젝트 연결 유지. 고객용 문서 포맷 교체는 제외한다.

기준: 고객사 입력 요소 21개·필수 1개(B01). 전체 경로의 직접 입력량과 왕복은 미측정(B06).

| 완료 | ID | 목표·통과 기준 | 검증 방법 | 현재 판정 |
|---|---|---|---|---|
| ☐ | F01-01 | 중복 입력 필드 0개 | 고객사 생성→단일 품목 견적 초안 저장에서 상호·담당자·프로젝트 재입력 집계 | 미검증 |
| ☐ | F01-02 | 고객사 등록을 위한 외부 화면 왕복 0회 | 견적 작업 안에서 고객사 등록 후 원래 입력·연결 보존 확인 | 미검증 |
| ☐ | F01-03 | 직접 입력 필드 수 기준 대비 30% 이상 감소(제안) | 동일 결과의 개선 전후 필드 수 비교. 기존 최소 경로가 더 짧으면 목표 재승인, 필수 검증 제거 금지 | 기준 미측정 |
| ☐ | F01-04 | 저장한 초안과 새로고침 후 고객사·품목·금액 일치 100% | 서버 저장 결과와 화면 값을 대조. 가상 데이터 전용 | 미검증 |

후속 구현 대상: clients-page-client.tsx, client-company-fields.tsx, quotes-page-client.tsx, quote-client-project-fields.tsx, 관련 actions와 회귀검사. 경로는 apps/web/src/components 및 해당 app/(private) 아래다.

## F02 · 고객사·견적 초안 보존

목표: 중단 후 다시 시작할 때 적은 내용을 잃지 않는다. 범위는 고객사 등록·견적 초안 두 폼이며 다른 폼 확장은 후속이다.

기준: PC 고객사 폼에 가상 상호를 입력하고 닫았다 열었을 때 빈 값으로 돌아옴(B02). 16개 전체 시나리오를 측정한 것은 아니다.

| 완료 | ID | 목표·통과 기준 | 검증 방법 | 현재 판정 |
|---|---|---|---|---|
| ☐ | F02-01 | 16/16 시나리오 입력 손실 0건 | 아래 조합별 문자열·날짜·선택·품목/수량/단가를 저장 전 값과 대조 | PC 닫기→재열기 실패 재현, 나머지 미검증 |
| ☐ | F02-02 | 명시적 초안 버리기 후 복원 0건 | 버리기→재열기·새로고침에서 이전 초안이 되살아나지 않음 | 미검증 |
| ☐ | F02-03 | 성공 저장 후 재전송으로 중복 문서 생성 0건 | 같은 제출 식별자로 재시도, 저장 결과 건수 대조 | 미검증 |
| ☐ | F02-04 | 다른 사용자·다른 폼의 초안 노출 0건 | 계정·폼 키 분리와 로그아웃 처리 검사. 공개 로그에 입력 본문 금지 | 미검증 |

16개 조합: 고객사/견적 × PC/실제 휴대폰 × (닫기→재열기, 새로고침, 다른 화면 이동→복귀, 저장 실패→복구). 저장 실패는 격리된 테스트에서 주입하며 운영 서버를 끄지 않는다. 기기 간 초안 동기화는 이번 범위가 아니다.

후속 구현 대상: create-panel.tsx와 위 두 폼, 초안 저장 계층, apps/web/tests의 관련 회귀검사. 초안 저장소와 보관·폐기 방식은 보안 검토 후 확정한다.

## F03 · 승인 찾기와 판단

목표: 올바른 대상을 찾아 충분한 근거로 결정한다. 빨리 승인하는 것만을 목표로 삼지 않는다.

기준: 사용자 시간·정확도 모두 미측정(B07). 이번 단계에서 승인·반려를 실제 실행하지 않았다.

| 완료 | ID | 목표·통과 기준 | 검증 방법 | 현재 판정 |
|---|---|---|---|---|
| ☐ | F03-01 | 지정 항목 탐색 중앙값 15초 이내, 오선택 0건(제안) | 가상 대기 10건 중 지시한 대상과 근거를 여는 데 걸린 시간, 5회 | 사용자 측정 대기 |
| ☐ | F03-02 | 고정 정답 5사례 오판 0건 | 올바른 승인/반려와 이유를 확인. 판단 시간은 전후 비교하되 단축을 통과 조건으로 강제하지 않음 | 사용자 측정 대기 |
| ☐ | F03-03 | 승인 전 대상·금액·증빙·결과 확인 가능 100% | 5사례 모두 실제 확정 직전 검토 정보 대조 | 미검증 |
| ☐ | F03-04 | 승인 없는 확정·중복 확정 0건 | 권한 없는 요청과 동일 요청 반복을 격리 검사 | 미검증 |

후속 구현 대상: approvals-page-client.tsx, 관련 상세 승인 actions, tests/approvals.test.ts. 사람이 잘못 눌러도 곧바로 외부 발송되는 흐름을 만들지 않는다.

## F04 · AI 답변·초안·실제 반영 구분

목표: AI가 말만 했는지, 저장할 후보를 만들었는지, 시스템에 실제 반영했는지 혼동하지 않는다.

범위: 상태 표시와 근거 연결. 실제 쓰기 도구는 별도 설계·승인 뒤에만 포함한다. 외부 발송·결제·권한 자동 변경은 제외한다.

| 완료 | ID | 목표·통과 기준 | 검증 방법 | 현재 판정 |
|---|---|---|---|---|
| ☐ | F04-01 | 6/6 상태 분류 정답, 각 5초 이내(제안) | 답변/초안/반영 완료 각 2사례를 섞어 대표가 분류. 연습은 별도 사례 | 사용자 측정 대기 |
| ☐ | F04-02 | 승인 전·실패 시 ‘반영됨’ 오표시 0건 | 거절·타임아웃·중복·서버 오류 사례에서 상태와 결과 대조 | 쓰기 미구현 / 미검증 |
| ☐ | F04-03 | 반영 결과 재조회 일치율 100% | 승인한 변경과 서버에 다시 조회한 기록의 ID·내용·이력 대조 | 쓰기 미구현 / 미검증 |
| ☐ | F04-04 | 사용자가 누르지 않은 중지의 오표시 0건 | 연결 단절·재열기와 명시적 중지를 구분하는 회귀검사 | 기존 구현 있음 / 이 계획으로는 미검증 |

후속 구현 대상: agent-chat.tsx, agents/chat-runs.ts, API chat route, 해당 tests. 상태는 모델의 문장이 아니라 서버가 확인한 실행 기록에서 온다.

## F05 · 대시보드 숫자·프로젝트 정확성

목표: 장식보다 신뢰 가능한 숫자와 연결을 먼저 확보한다. 실제 세무 판정과 은행 연동은 제외한다.

| 완료 | ID | 목표·통과 기준 | 검증 방법 | 현재 판정 |
|---|---|---|---|---|
| ☐ | F05-01 | 월간 예정 금액 오차 0원 | 28/29/30/31일 월말과 윤년, 연도 전환·경계 날짜를 고정 fixture로 계산 | 9월 29일 100원 누락 재현(B03) |
| ☐ | F05-02 | 청구·입금 예정·실제 입금 분류 오류 0건 | 청구월≠입금 예정월, 예정/확인 상태를 조합. 날짜와 상태 정의를 먼저 고정 | 다음 달 입금 예정이 이번 달 집계에 포함(B04) |
| ☐ | F05-03 | 프로젝트 간 단계·다음 행동 혼입 0건 | 같은 고객사의 두 프로젝트와 동명 고객사 사례, projectId 기반 정답 대조 | 동일 고객사 두 프로젝트 모두 견적 있음(B05) |
| ☐ | F05-04 | 기록 없음과 정상 상태 혼동 0건 | 빈 데이터·연체·업무 지연 각각의 표시와 판단 근거 대조 | 미검증 |

후속 구현 대상: apps/web/src/lib/domain/dashboard.ts, lib/dashboard/repository.ts, app/(private)/dashboard/page.tsx, tests/dashboard.test.ts.

## F06 · 회사·자체 사업 업무

목표: 고객사 프로젝트가 없어도 회사 운영과 자체 앱 업무를 담을 수 있게 한다.

| 완료 | ID | 목표·통과 기준 | 검증 방법 | 현재 판정 |
|---|---|---|---|---|
| ☐ | F06-01 | 회사/자체 사업/고객사 프로젝트 3유형 업무 등록·재조회 3/3 | 관련 연결을 유지한 가상 업무, 고객사 없는 유형은 가짜 고객사 생성 불필요 | 미검증 |
| ☐ | F06-02 | 잘못된 유형 간 연결·무권한 조회 0건 | 워크스페이스·사업·프로젝트 범위 음성 사례 | 미검증 |

이 기능은 후속 계획 후보다. 이번 단계 또는 관리 화면 구현 승인에 DB 변경 권한이 포함되지 않는다.

## F07 · 사람이 보는 개선 계획·검증 화면

목표: 대표가 기능별 목표·실제 결과·남은 일을 쉽게 찾는다. 기존 매뉴얼 진행 현황을 확장하며 별도의 PM 제품을 만들지 않는다.

범위: /admin/manual/progress와 기존 매뉴얼 입구 확장. 목록은 기능명·목표·진행 단계·통과 수·실패/재검증 수·다음 행동을 보여주고 상세에서 계획·검사·증거를 펼친다. 모바일은 세로 배치. 체크 하나로 근거 없이 완료시키는 편집기는 제외한다.

| 완료 | ID | 목표·통과 기준 | 검증 방법 | 현재 판정 |
|---|---|---|---|---|
| ☐ | F07-01 | 기능 목록에서 체크리스트까지 2번 이내 | 클릭/탭 횟수 측정, PC·모바일 각각 | 미구현 |
| ☐ | F07-02 | 등록 검사 누락 0개 | 전체 원본 ID 집합=화면 검사 ID 집합. 결과가 없는 검사도 남음 | 미구현 |
| ☐ | F07-03 | 근거 없는 완료·미검증의 통과 집계 0건 | 결과 없음·실패·증거 없음·다른 버전·다른 환경 입력 사례 | 미구현 |
| ☐ | F07-04 | 새 결과·관련 변경 후 상태 계산 정확도 100% | 통과→관련 변경→재검증→새 통과, 이전 결과 이력 보존 | 미구현 |
| ☐ | F07-05 | 무권한 열람 0건, 공개 산출물 비밀 노출 0건 | 대표 인증 관문 검사, 공개 결과 민감정보 검사 | 미구현 |
| ☐ | F07-06 | 주요 조작 가림·의도치 않은 가로 넘침 0건 | 접힌/펼친 폴드 실제 폭과 PC. 터치·대비는 공용 기준과 제품 토큰 검사 | 미구현 |

후속 구현 대상: admin/manual/progress/page.tsx, lib/admin-manual/repository.ts, lib/domain/admin-manual.ts, 공통 프레임·토큰, 관련 tests. 구조화가 필요하면 이 계획 원본을 변환하고 옛 상태 사본을 남기지 않는다. 기존 system-progress의 ‘완료’는 검증 통과로 자동 변환하지 않는다.

## 실행 순서와 중단 지점

| 단계 | 결과물 | 다음 단계 관문 |
|---|---|---|
| 1 — 현재 | 이 계획·공용 변경안·기준 측정 기록 | 목표와 미측정 항목의 준비 방식 승인 |
| 2 | F07 조회 화면 최소 구현 | Sonnet 구현, Codex 통합 리뷰, 가상 상태·실제 화면 검증 |
| 3 | F05 정확성·F02 초안 보존 | 실패 재현→최소 수정→회귀검사. 대표 사용 환경 확인 |
| 4 | F01·F03 흐름 개선 | 개선 전후 사용자 측정·데이터 일치 확인 |
| 5 | F04 상태·결과 연결 | 쓰기 도구가 필요하면 별도 범위 승인 |

단계마다 독립적으로 확인할 수 있는 묶음으로 끝낸다. 범위나 시간이 커지면 다음 수정 전에 이유·남은 작업을 보고한다. 상한을 채우기 위해 작업하지 않는다. 사람 측정 또는 권한이 없으면 대기 상태를 표시하며 대신 성공으로 체크하지 않는다.

## 보존할 후속 후보

F06 회사·자체 사업 업무, 반복 일정·운영 캘린더, Recho 기록 수집, 정산 CSV 대조, 백업·복원 검증. 각각 별도 목표와 검사 목록을 승인받은 뒤 구현한다.

## 이 단계 준비 체크리스트

- [x] 공용 원문 순서대로 재확인, 두 저장소 최신 main·공개 여부 확인
- [x] 공용 변경안과 제품 계획을 분리
- [x] 개선 전 비파괴 측정 및 실패 재현 기록
- [x] 미측정 사유와 후속 측정 절차 명시
- [ ] 전체 고객사→견적 경로 기준값 측정
- [ ] 실제 휴대폰 초안 기준 측정
- [ ] 대표의 승인 판단·AI 상태 이해도 기준 측정
- [ ] 목표 최종 고정·후속 구현 승인

적용 기준: 계획·증거의 단일 원본, 구현/검증/반영 구분, 공용 디자인 기준과 코어룸 토큰. 실제 화면 구현·접근성 통과를 이 문서 작성만으로 주장하지 않는다.
