# Coreloom 작업 지침

이 파일은 AI·개발자가 **일하는 방식**이다. 제품의 지금 규칙은 [RULES.md](RULES.md)다. 둘을 섞지 않는다.

## 0. 작업 시작 규칙

Coreloom에서 무엇을 하든 먼저 [RULES.md](RULES.md)를 읽는다. 그 파일은 지금 이 제품이 어떻게 동작해야 하는지의 정본이다.

- 새 작업이나 작업 종류가 바뀌면 현재 `RULES.md`를 다시 읽는다. 이 파일의 「설계할 때」와 「배운 것」도 같이 본다.
- 새 작업은 반드시 최신 `origin/main`에서 새 브랜치를 딴다. 이미 `main`에 머지된 옛 브랜치에 이어서 커밋하지 않는다. `cursor/project-tasks-schedule-0ce2`는 그 예다.
- 시스템 구조와 운영 방법은 [manual/00-coreloom-매뉴얼.md](manual/00-coreloom-매뉴얼.md)가 정본이다.
- 실제 기능·규칙·운영 방법을 바꾸면 관련 매뉴얼과 [manual/CHANGELOG.md](manual/CHANGELOG.md)를 함께 갱신한다.
- 문서와 규칙만 바꾼 작업은 링크·내용을 직접 확인한다. 구현·배포 여부를 추정해 완료로 말하지 않는다.

## 1. 범위와 안전

- 요청한 업무 흐름과 데이터를 먼저 고정하고, 인접 기능을 임의로 추가하지 않는다.
- 재무·거래처·구성원·첨부 파일은 회사 민감 정보다. 실데이터, Funnel 주소, 고객사명, 이메일, 실개인정보는 예제·로그·커밋·PR·채팅에 넣지 않는다.
- 금액, 승인, 권한, 삭제는 변경 이력 없이 수정하거나 지우지 않는다.
- 운영 배포 전에는 역할별 접근, 승인 이력, 주요 데이터 흐름을 실제 화면 또는 API로 확인한다.
- 운영 화면 셸의 **지금 동작**은 `RULES.md` 8절이다. 그 절을 다른 면에 베끼거나, 앞으로 영원히 같다고 닫지 않는다. 생각은 「설계할 때」를 따른다.

## 2. 매뉴얼과 관리자 화면

- 매뉴얼의 원본은 저장소 `manual/`의 Markdown 파일이다.
- 미래 배포본의 관리자 화면 `/admin/manual`은 이 원본을 읽기 전용으로 표시한다. 화면용 사본을 별도로 관리하지 않는다.
- 관리자 화면에는 현재 배포 버전과 매뉴얼 원본의 Git 커밋을 함께 표시한다.
- 운영자가 화면에서 매뉴얼 변경을 요청할 경우에도, 변경은 검토·커밋·배포 과정을 거쳐야 한다.

## 3. 개발 PC와 스키마

- 개발 Windows PC의 로컬 저장소는 `C:\dev\Coreloom`이다. 정식 기동은 `apps/web`에서 `npm run up`이다. 로컬은 HTTP `http://127.0.0.1:3000`만 쓰고, 로그인 Origin은 `localhost`와 같게 본다. 휴대폰 Funnel은 HTTPS `:8443`이다. 숫자 없는 `443`은 끄고, `:10000`은 다른 앱이므로 건드리지 않는다. 상세는 매뉴얼 정본을 따른다.
- 스키마를 바꾸면 같은 변경에 drizzle 마이그레이션을 둔다. 개발 PC에서 `npm run db:migrate`(또는 `tsx scripts/migrate.ts`)가 필요하면 그 안내를 빠뜨리지 않는다.

## 4. 설계할 때

`RULES.md`는 지금 이 제품의 상태다. 새 면·새 동작을 만들 때는 그 문장을 외워 복붙하지 말고, 그 면의 역할로 다시 고른다.

자리를 차지할지, 위에 띄울지부터 본다. 이 제품에서 에이전트 패널은 지금은 플로팅이지만, 자리를 차지하는 쪽이 맞을 수도 있다. 맞다고 판단하면 그렇게 제안하고, 대표가 오케이하면 `RULES.md`를 바꾼다.

한 면의 디테일(밀기, 호버 미리보기, 버튼 자리, 레이어)을 다른 면에 그대로 옮기지 않는다. 버튼을 면과 따로 고정하지 않는지, 본문이 위아래로 뛰는지, 호버가 터치에도 켜지는지, 슬라이드가 잘리는지, 레이어 순서가 `RULES.md` 8절과 맞는지 그 작업에서 다시 본다.

## 5. 작업이 끝나면

화면·셸·동작·규칙을 대표가 오케이하면, 커밋하기 전에 **처음 안**과 **최종 오케이**를 비교한다. 오타·링크만 고친 커밋은 건너뛴다.

비교한 뒤 나눈다.

- **제품이 바뀌었으면** `RULES.md`와 해당 매뉴얼·`manual/CHANGELOG.md`·필요하면 `manual/system-progress.md`를 같은 커밋에서 고친다.
- **실수했거나 디테일을 빠뜨렸으면** 이 파일 「배운 것」을 같은 커밋에서 고친다.

「배운 것」에는 만능 법칙을 올리지 않는다. 다음에 빠뜨리지 않을 질문으로 짧게 적는다. 비슷한 항목은 합치고, 길어지면 압축한다. 일기처럼 쌓지 않는다. 제품의 지금 상태는 여기에 복사하지 않는다.

## 6. 배운 것

### 면의 자리

- **빠뜨리기 쉬운 것:** 이 면은 자리를 차지하는가, 위에 뜨는가. 버튼이 면과 같이 움직이는가. 본문이 위아래로 뛰는가. 레이어 순서는 맞는가. 호버가 터치·좁은 화면에도 켜지는가.
- **잘못:** 「공통 원리」로 좌측 내비의 밀기·호버·버튼 두 자리를 다른 면에 복사하려 했다. 이 제품의 에이전트 패널이 플로팅인 것을, 「에이전트는 영원히 자리를 차지하면 안 된다」는 생각의 법칙으로 `RULES.md`에 올렸다.
- **다음부터:** 지금 상태는 `RULES.md` 8절을 따른다. 새 면은 그 면의 역할로 다시 고른다. 에이전트 패널이 자리를 차지하는 쪽이 맞으면 그렇게 말하고, 오케이되면 제품 규칙을 바꾼다.
