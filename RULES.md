# Coreloom 운영 규칙

> 회사 운영에 쓰이는 데이터와 권한을 안전하고 추적 가능하게 관리하기 위한 단일 원본입니다.

## 1. 제품의 역할

Coreloom은 회사의 사람·거래처·프로젝트·업무·승인·매출·비용을 연결하는 운영 ERP다. 회계·세무 시스템을 임의로 대체하거나 법적 판단을 자동화하지 않는다.

## 2. 데이터 원칙

- 모든 금액은 통화와 발생일·예정일·확정 상태를 함께 가진다.
- 매출과 비용은 거래처 또는 프로젝트와 연결하지 못하면 `미분류`로 명확히 표시한다.
- 증빙 파일은 원본을 보존하고, 누가 언제 올렸는지 기록한다.
- 민감 정보는 역할별로 최소한만 보여 준다.
- 실데이터, 비밀번호, API 키, 개인 식별 정보, Funnel 주소, 고객사명, 이메일은 코드·문서·테스트 고정값·채팅·로그·커밋·PR에 넣지 않는다.
- Recho에서 온 메일·통화·회의는 원문 식별자·발생 시각·연결 이유를 보존한다. 요약만을 정본으로 삼거나 원문을 Coreloom에서 수정하지 않는다.
- 견적서, 계약서, 청구서, 발행된 세금계산서는 덮어쓰지 않는다. 수정은 새 버전 또는 수정 발행으로 남긴다.

## 3. 권한과 승인

- 권한은 대표/관리자, 팀원, 회계 담당자처럼 업무 역할로 부여한다.
- 승인·반려·취소는 실행자, 시각, 사유를 남긴다.
- 승인된 금액이나 정산 상태의 직접 수정은 금지한다. 정정 기록 또는 취소 후 재등록으로 남긴다.
- 삭제는 복구 가능하게 처리하며, 관리자만 수행할 수 있다.
- 이메일 발송, 전자서명 요청, 세금계산서 발행은 대표 승인 뒤에만 실행한다. 외부 결과를 받지 못하면 완료로 표시하지 않는다.

## 4. AI 에이전트 권한

- AI 에이전트는 사람 계정과 구분된 시스템 계정으로 등록한다.
- 각 에이전트에는 목적, 허용 업무, 접근 가능한 사업·프로젝트·데이터 범위, 일하는 방식·답변 방식·절차·지침, 구독 모델(Claude/GPT·Codex/Cursor 등, API 키 호출 아님)을 명시한다.
- 기록 저장, 외부 발송, 금액·계약 확정, 권한 변경 능력은 기본으로 꺼 둔다. 대표가 에이전트별로 명시한 능력만 실행할 수 있으며, 지침 문구만으로 권한은 생기지 않는다.
- 에이전트의 요청·입력 자료·결과·사람 승인 여부·사용한 능력을 작업 이력으로 남긴다.
- 대표가 건별로 승인하거나 해당 에이전트에 능력이 켜져 있지 않으면 자동 결제·자동 이체·자동 외부 발행을 실행하지 않는다.
- 근거가 없는 AI 요약은 공식 결정 후보로 올리지 않는다.

## 5. 변경 이력과 검증

- 업무, 프로젝트, 매출, 비용, 승인, 권한의 생성·변경·삭제에는 변경 이력을 남긴다.
- 새 기능은 정상 흐름과 권한 없는 사용자의 차단 흐름을 각각 확인한다.
- 배포 전에는 해당 기능의 데이터 저장, 화면 표시, 새로고침 후 상태 유지, 변경 이력을 한 흐름으로 확인한다.
- 실제 운영에 연결되지 않은 로컬 확인은 운영 반영으로 표현하지 않는다.

## 6. 문서의 단일 원본

- 운영 규칙은 이 `RULES.md`, 운영 설명은 `manual/`, 제품 범위는 `docs/`에 둔다.
- 같은 규칙이나 절차를 여러 문서·화면에 복사하지 않는다. 화면은 정본을 읽어 표시한다.
- 구현·운영 방식이 바뀌면 같은 변경에서 해당 매뉴얼과 변경 기록을 갱신한다.
- 기능을 추가하거나, 기능의 구현 상태가 바뀌면, 같은 커밋에서 `manual/system-progress.md`의 진행 현황 표를 갱신한다.

## 7. 관리자 매뉴얼 노출 계약

미래 배포본은 `/admin/manual`에서 다음을 읽기 전용으로 제공해야 한다.

1. `manual/00-coreloom-매뉴얼.md`
2. `manual/system-progress.md`
3. `manual/CHANGELOG.md`
4. 역할별 운영 절차 문서
5. 현재 배포 버전과 매뉴얼 원본 커밋

이 화면은 관리자만 볼 수 있어야 하며, 애플리케이션에 복사한 HTML을 별도 정본으로 두지 않는다.

## 8. 화면 셸

대표 운영 화면은 접힌 폴드, 펼친 폴드, 일반 모니터(4K 포함)에서 같은 메뉴를 제공한다.

- 좁은 화면(접힌 폴드·일반 휴대폰)은 하단 메뉴바를 쓰고 본문을 위에 둔다. 그룹 항목은 아래에서 시트로 연다.
- 넓은 화면(펼친 폴드·태블릿·데스크톱·4K)은 좌측 사이드바를 쓴다. 사이드바가 열리면 왼쪽에서 슬라이드해 들어오고 본문 위에 뜨지 않으며 본문을 옆으로 민다. 페이지를 옮겨도 연 상태를 유지한다.
- 넓은 화면에서 접기/펼치기 버튼은 하나이며 화면 왼쪽 위 같은 자리에 둔다. 사이드바를 접거나 펼 때 본문은 옆으로만 밀리고 위아래로 움직이지 않는다.
- 넓은 화면에서 접힌 사이드바는 접기 버튼에 포인터를 올리면 임시로 본문 위에 뜨고, 벗어나면 사라진다. 클릭이나 Windows의 Ctrl+B, Mac의 Command+B로 연 상태는 본문을 옆으로 민다.
- 전역 AI 에이전트 패널은 우측에 두며, 좁은 화면에서는 하단 시트로 연다. Ctrl+J 또는 Command+J로 여닫고, `/agents`에 등록된 활성 에이전트를 고른다. 목록 화면의 생성은 `+` 아이콘으로 열고, 작성 폼은 패널로 띄운다.