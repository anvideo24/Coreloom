# Coreloom 운영 규칙

> 이 파일은 **이 제품만의 규칙**이다. 공용 제품 규칙은 GitHub `anvideo24/working-method`의 `RULES.md`다. 공용을 이 파일에 복사해 두고 고치지 않는다. 겹치면 이 파일이 Coreloom의 정본이다.

일하는 방식은 `working-method`의 `HOW.md`와 `LESSONS.md`다. 이 저장소 `AGENTS.md`는 그 정본을 가리키고 Coreloom 기동만 적는다.

## 0. 🔴 이 저장소는 공개다

2026-09-05부터 `anvideo24/Coreloom`은 **공개 저장소**다. 코드·문서·커밋 메시지·PR 본문을 **누구나 읽는다.** 지운 뒤에도 복제·색인이 남으므로 되돌릴 수 없다.

- 여기 들어가는 실데이터·비밀·고객사명·이메일은 「나중에 정리할 것」이 아니라 **영구 노출**이다.
- 공개로 바꾼 이유는 `main` 병합에 검사 통과를 조건으로 걸기 위해서다. 비공개 저장소에서는 무료 플랜으로 그 기능을 못 쓴다.
- 감수한 것: 공개 전 커밋 26개에 개인 이메일이 작성자 정보로 남아 있다. 대표가 알고 결정했다.

## 1. 제품의 역할

Coreloom은 회사의 사람·거래처·프로젝트·업무·승인·매출·비용을 연결하는 운영 ERP다.

## 2. 이 제품만의 데이터

- Funnel 주소는 코드·문서·테스트 고정값·채팅·로그·커밋·PR에 넣지 않는다. 저장소가 공개라 실주소는 곧 침입 경로다.
- Recho에서 온 메일·통화·회의는 원문 식별자·발생 시각·연결 이유를 보존한다. 요약만을 정본으로 삼거나 원문을 Coreloom에서 수정하지 않는다.

## 3. 문서와 관리자 화면

- 이 제품의 운영 설명은 `manual/`, 제품 범위는 `docs/`에 둔다.
- 기능을 추가하거나, 기능의 구현 상태가 바뀌면, 같은 커밋에서 `manual/system-progress.md`의 진행 현황 표와 `manual/CHANGELOG.md`를 갱신한다.
- 운영자가 화면에서 매뉴얼 변경을 요청해도, 변경은 검토·커밋·배포를 거친다.
- `/admin/manual`은 **고르는 입구**다. 긴 원본을 홈에 펼치지 않고, 칸을 고른 뒤에 연다.
- `/admin/manual`에서 다음을 읽기 전용으로 제공한다.
  1. 공용 규칙 — `working-method`의 `RULES.md`
  2. 일하는 방식 — `working-method`의 `HOW.md`
  3. 배운 것 — `working-method`의 `LESSONS.md`
  4. 이 제품 규칙 — 저장소 뿌리의 `RULES.md`
  5. 운영 설명 — `manual/00-coreloom-매뉴얼.md`
  6. `manual/system-progress.md`
  7. `manual/CHANGELOG.md`
  8. 역할별 운영 절차 문서
  9. 현재 배포 버전과 매뉴얼 원본 커밋
  10. 개선 목표·검증 현황 — 계획서와 결과 파일
      - 원본 둘: 계획 `docs/superpowers/plans/2026-09-05-measurable-improvements.md`, 결과 `docs/quality/verification-results.json`.
      - 결과 파일은 덧붙이기만 한다. 지우지 않는다.
      - 계획서의 「현재 판정」 칸은 계획 당시 값이다. 현재 상태는 결과 파일에서만 본다.
      - `manual/system-progress.md` 구현 현황 표의 「완료」를 검증 통과로 승격하지 않는다.
      - 통과 수는 검사 현황이지 개발 완료율이 아니다.
      - 판정 규칙의 정본은 `apps/web/src/lib/domain/verification-plan.ts`다.
      - 목표가 셀 수 있는 수를 요구하면 결과에 몇 개를 쟀는지 함께 적는다. 덜 쟀으면 통과로 세지 않는다.
- 공용 정본은 이 저장소와 같은 부모 폴더의 `working-method` 클론에서 읽는다. 다른 자리에 두면 환경 변수 `CORELOOM_WORKING_METHOD_DIR`로 알린다. **그 변수를 넣으면 그 자리만 본다** — 값이 틀렸을 때 옆 폴더로 넘어가면 잘못 적은 설정이 안 드러난다.
- 원본을 찾지 못하면 그 칸은 **「아직 없다」고 밝힌다.** 빈 화면으로 두거나 내용이 없는 것처럼 보이게 하지 않는다.
- 어느 칸이 열리는지는 **저장소가 아니라 파일 단위로** 센다. 저장소만 보고 판정하면 홈에서는 링크로 보이는데 눌러 보면 없다고 나온다.
- 공용 문서를 이 저장소로 복사해 두지 않는다. 읽기만 하고, 고칠 것은 `working-method`에서 고친다.
- 이 화면은 관리자만 볼 수 있어야 하며, 애플리케이션에 복사한 HTML을 별도 정본으로 두지 않는다.
- 지금 범위는 읽기 전용이다. 화면에서 바로 저장하는 편집기를 만들 때, 저장은 git 커밋이어야 한다. DB에만 두고 git에 남기지 않는 방식은 쓰지 않는다.

## 4. 화면 셸

대표 운영 화면은 접힌 폴드, 펼친 폴드, 일반 모니터(4K 포함)에서 **같은 메뉴**를 제공한다. 메뉴 구성은 바꾸지 않고, 화면 폭에 따라 껍데기만 바꾼다. 640px 미만은 하단 메뉴, 640px 이상은 좌측 내비를 쓴다. AI 대화는 1200px 미만에서 전체 화면, 1200px 이상에서 본문 공간을 지키는 우측 도킹으로 바뀐다.

### 자리를 차지하는 면, 위에 뜨는 면

지금은 이렇게 나눈다.

- **자리를 차지한다:** 넓은 화면에서 고정 연 좌측 사이드바. 본문을 옆으로 민다.
- **위에 뜬다(플로팅):** 1200px 미만의 전역 AI 에이전트 패널, 작성 패널, 좌측 내비의 호버 미리보기, 좁은 화면의 아래 시트. 본문 폭을 줄이지 않는다.

전역 AI 에이전트 패널은 1200px 미만에서 전체 화면 모달이고, 1200px 이상에서는 우측에 도킹해 자리를 차지한다.

현재 셸에서는 아래를 지킨다.

- 한 동작이 밀기와 덮기를 동시에 하지 않는다.
- 움직이는 면 위의 버튼은 그 면과 같이 움직인다. 면만 슬라이드하고 버튼을 화면에 고정하면 겹친다.
- 크롬을 여닫을 때 제목과 본문이 위아래로 뛰면 안 된다.
- 레이어 순서는 제품 규칙이다. 새 면을 띄우기 전에 이 절의 순서를 본다.
- 호버는 정밀 포인터 전용이다. 터치와 좁은 화면은 탭·시트·버튼으로 연다.
- 슬라이드는 칸 너비나 overflow에 잘려 팝처럼 보이면 안 된다.
- 폭이 바뀐다고 메뉴·필드·권한이 달라지면 안 된다.

### 좁은 화면

접힌 폴드와 일반 휴대폰은 하단 메뉴바를 쓰고 본문을 위에 둔다. 그룹 항목은 아래에서 시트로 연다. 좌측 사이드바와 호버 임시 메뉴는 쓰지 않는다.

### 넓은 화면 좌측 내비게이션

640–1199px의 펼친 폴드·태블릿은 기본 64px 레일을 쓰고, 메뉴를 명시적으로 열면 본문 위 오버레이로 탐색한다. 1200px 이상의 데스크톱·4K는 사용자가 저장한 240px 사이드바 선택을 따른다. AI 도킹으로 본문이 좁아질 때만 64px 레일과 오버레이 탐색으로 임시 전환하며 저장 선택은 바꾸지 않는다. 열림과 미리보기는 다른 동작이다.

**고정 열림**은 자리를 차지한다.

- 폭은 240px이다.
- 왼쪽에서 슬라이드해 들어오고, 본문 위에 덮지 않으며 본문을 옆으로 민다.
- 접기 버튼은 사이드바 안에 있으며 사이드바와 같이 움직인다.
- 클릭, Esc, Windows의 Ctrl+B, Mac의 Command+B로 여닫는다.
- 페이지를 옮겨도 연 상태를 유지한다.

**호버 미리보기**는 자리를 차지하지 않는다.

- 사이드바가 접혀 있을 때만 쓴다.
- 펼치기 버튼에 포인터를 올리면 왼쪽에서 슬라이드해 본문·에이전트 패널·작성 패널보다 위, 최상단 레이어로 뜬다.
- 버튼과 메뉴 밖으로 나가면 잠시 뒤 사라진다. 페이지를 옮기면 미리보기는 끄고, 고정 열림은 유지한다.

**접기/펼치기 버튼**은 같은 컨트롤을 두 자리에 둔다.

- 접혔을 때: 화면 왼쪽 위 레일의 펼치기 버튼.
- 열렸을 때: 사이드바 안 CORELOOM 옆의 접기 버튼. 사이드바와 같이 슬라이드한다.
- 한 버튼을 화면 왼쪽 위에 고정해 두고 사이드바가 그 아래로 들어오게 하지 않는다.

### 우측 패널

전역 AI 에이전트 패널은 1200px 미만에서 전체 화면 모달로 열고 배경을 조작할 수 없게 한다. 1200px 이상에서는 우측에 400–512px로 도킹한다. 본문은 최소 640px를 남기며, 240px 좌측 내비가 그 공간을 침해할 때만 저장 상태를 바꾸지 않고 64px 레일로 임시 축소한다. 닫으면 사용자의 원래 내비 선택이 다시 보인다. 입력 키보드가 올라와도 패널 헤더(닫기)와 입력은 시각 뷰포트 안에 남긴다. Ctrl+J 또는 Command+J로 여닫고 `/agents`에 등록된 활성 에이전트를 고른다. 헤더 아래에는 새 대화·대화 이력·설정 도구를 두고, 입력의 이미지 추가·구독 모델·전송은 한 행에 둔다. 설정은 대화 방식과 자료 접근을 나누며, 새 조회 권한을 켜 저장할 때 허용 범위를 다시 확인한다. 나머지 대화 복원·중지·복사·조회 권한·구독 실행 경계는 아래 운영 설명을 따른다. 외부 발송·수정·삭제·금액 확정 도구는 제공하지 않고 API 키로 자동 전환하지 않는다. 작성 패널은 계속 본문 위에 뜨며, 기존 닫기 동작과 폭을 유지한다.

### 레이어 순서

대화 패널은 재열기·새로고침 시 마지막 대화를 복원한다. 직접 첨부한 이미지는 서버에서 검증·축소해 비공개로 보관하고 선택한 구독 모델에 전달한다. 문서함 원본 조회 권한과는 별개이며, 이미지 안의 문구가 권한을 부여하지 않는다. 구체적인 입력·보관 한도와 복원 범위는 운영 매뉴얼의 에이전트 구독 대화 절에서 관리한다.

위에서부터 아래 순서를 지킨다. 새 면을 넣을 때 이 순서를 바꾸려면 이 절을 같이 고친다.

1. 좌측 내비 호버 미리보기
2. 작성 패널
3. 전역 AI 에이전트 패널
4. 본문
5. 고정 연 좌측 사이드바는 레이어가 아니라 자리이며, 본문을 옆으로 민다.
